# ASSALKOM_IMPLEMENTATION_REPORT

## الحالة

هذا التقرير أُنشئ بعد اكتمال القراءة والجرد، ويُحدَّث بعد كل مهمة مستقلة. كل صف يمثل صورة واحدة وملف شرح مطابقًا واحدًا. تُسجل المهمة فقط بعد اختبارها وتوثيق مراجعتها البصرية؛ وقد أُغلقت 001–013 سلوكيًا مع حجب بصري موثق حيث ثبت، وأُضيفت نتائج التنفيذ بالتسلسل.

## قواعد الإثبات

لا تُسجل «نجاح» إلا بعد تعديل نطاق المهمة فقط، تشغيل اختبارها، تشغيل التحليل المناسب، وفحص screenshot/golden على viewport ثابت. لا تُسجل مزامنة أو قبول جهاز/إطلاق خارجي دون دليل قابل لإعادة الإنتاج. لا تغيير في DB/API/RLS/الصلاحيات دون سبب وأثر وبديل وتوثيق في `ASSALKOM_GAPS.md`.

## سجل المهام

| المهمة | الأصل/المسار | الهدف والدور | مكونات/مسارات معدلة فعليًا | مصدر البيانات/API الفعلي | الإجراءات والانتقالات | الحالات المنفذة | الاختبار/الأمر | المقارنة البصرية | الانحرافات المتبقية | الحالة |
|---:|---|---|---|---|---|---|---|---|---|---|
| 001 | `screens/001_startup.png` + `explanations/001_startup.md` | شاشة بدء التطبيق — زائر / عميل — هاتف حقيقي — مرجع 5.1 | `apps/mobile_flutter/lib/app/assal_startup.dart`; `apps/mobile_flutter/lib/app/assal_app.dart`; `apps/mobile_flutter/test/task001_startup_test.dart`; `apps/mobile_flutter/test/visual_reference/task001_startup_reference.png`; `apps/mobile_flutter/test/visual_reference/task001_startup_reference_360x640.png` | لا استدعاء بيانات جديد؛ البوابة تغلف `AssalHomeShell` بعد تهيئة repository الموجودة | يظهر «عسلكم» و«من اليمن .. طبيعة أصيلة» و«جارٍ تجهيز سوق العسل...» ثم انتقال غير حاجب إلى الغلاف الرئيسي بعد 900ms؛ مسار `startupError` لم يتغير | startup view + transition؛ اختبار الانتقال نجح | `flutter test test/task001_startup_test.dart`: اختبار الانتقال نجح، واختبار golden فشل بنسبة اختلاف 100% بسبب اختلاف عرض SVG/الشعار في harness؛ `flutter analyze --no-pub`: No issues found | مرجع 1440×2560 ومرجع golden منطقي 360×640؛ الهوية/الخلية/الموضع الأساسي مطبقة، لكن الشعار لا يظهر في renderer الخاص بـSVG ضمن golden | لا تغيير DB/API/RLS/الصلاحيات؛ لا يُعتمد PNG المشتق ذي الخلفية السوداء؛ يلزم حل asset-rendering أو اعتماد harness موثق ثم إعادة المقارنة | جزئي — وظيفي ناجح، بصري محجوب |
| 002 | `screens/002_startup_error.png` + `explanations/002_startup_error.md` | خطأ بدء التشغيل — زائر / عميل — هاتف حقيقي — مرجع 5.2 | `apps/mobile_flutter/lib/app/assal_app.dart`; `apps/mobile_flutter/test/task002_startup_error_test.dart`; `apps/mobile_flutter/test/visual_reference/task002_startup_error_reference_360x780.png` | لا استدعاء بيانات جديد؛ يعرض رسالة `messageAr` الممررة، ولا يغيّر تهيئة Supabase أو مسار Production | يعرض حالة «غير متصل»، زر «تحديث» و«إعادة المحاولة» مع callback اختباري حقيقي، وبطاقة عربية ووسم تفسير؛ لا تنفيذ لزر حفظ/إلغاء الشكلي الوارد في اللوحة المتعارضة | حالة خطأ عربية + retry؛ التفاعل نجح في الاختبار الأول | `flutter test test/task002_startup_error_test.dart`: 1 اختبار وظيفي نجح، واختبار golden فشل بنسبة اختلاف 100%؛ `flutter analyze --no-pub`: No issues found | المصدر 1440×3120 ولوحة المرجع تحوي محتوى علويًا عريضًا ومساحة فارغة، بينما شاشة التطبيق responsive؛ لم تُسجل المطابقة نجاحًا | لا تغيير DB/API/RLS/الصلاحيات؛ تعارض PNG/شرح موثق، والـ golden يحتاج قرار أولوية أو مرجع لقطة هاتف حقيقية | جزئي — وظيفي ناجح، بصري محجوب بتعارض الأصل |
| 003 | `screens/003_auth_email.png` + `explanations/003_auth_email.md` | تسجيل الدخول بالبريد — زائر / عميل — هاتف حقيقي — مرجع 6.1 | `apps/mobile_flutter/lib/features/customer/customer_account.dart`; `apps/mobile_flutter/test/task003_auth_email_test.dart`; `apps/mobile_flutter/test/navigation_test.dart`; `apps/mobile_flutter/test/visual_reference/task003_auth_email_reference.png`; `apps/mobile_flutter/test/visual_reference/task003_auth_email_reference_360x640.png` | `requestEmailOtp(email)` ثم نافذة `verifyEmailOtp(email, token)` الموجودة؛ لا API جديد | البريد، التحقق من صيغة البريد، «إرسال رمز التحقق»، «إنشاء حساب»، «المساعدة» المرتبطة بمركز المساعدة، وانتقال OTP الحقيقي | حالة البريد الفارغ/غير الصالح؛ الحالة العادية؛ انتقال OTP؛ التحميل الموجود محفوظ | `flutter test test/task003_auth_email_test.dart`: الاختبار الوظيفي غير الصالح والانتقال إلى OTP نجحا، واختبار golden فشل بنسبة اختلاف 100% بسبب SVG؛ `flutter analyze --no-pub`: No issues found | ترتيب النصوص والأفعال طُبق، لكن شعار SVG لا يظهر في golden renderer كما في PNG المرجعي 1440×2560 | لا تغيير DB/API/RLS/الصلاحيات؛ يلزم حل asset-rendering/harness قبل اعتبار المقارنة نجاحًا | جزئي — وظيفي ناجح، بصري محجوب |
| 004 | `screens/004_auth_otp.png` + `explanations/004_auth_otp.md` | نافذة رمز التحقق OTP — زائر / عميل — هاتف حقيقي مع نافذة داخلية — مرجع 6.2 | `apps/mobile_flutter/lib/features/customer/customer_account.dart`; `apps/mobile_flutter/test/task004_auth_otp_test.dart`; `apps/mobile_flutter/test/visual_reference/task004_auth_otp_reference_360x640.png` | `requestEmailOtp`, `resendEmailConfirmation`, `verifyEmailOtp`/`verifyEmailConfirmation` الحالية؛ لا API جديد | نافذة OTP بعنوان «تحقق من بريدك»، رسالة الإرسال، بريد مقنّع، «تعديل البريد» الذي يفتح الحقل الحقيقي، إدخال 6–9 أرقام، «إعادة إرسال الرمز» مع عداد، وزر «تحقق»؛ المؤقت يُلغى عند الإغلاق/النجاح | تحميل/عداد إعادة الإرسال؛ بريد مقنّع؛ وضع تحرير البريد؛ خطأ البريد/الرمز؛ نجاح التحقق؛ اختبار العناصر نجح | `flutter test test/task004_auth_otp_test.dart`: الاختبار الوظيفي نجح، وgolden فشل بنسبة اختلاف 100%؛ `flutter analyze --no-pub`: No issues found | السلوك والنصوص الأساسية مطبقة داخل Dialog، لكن PNG 004 يتضمن إطار هاتف وخلفية تطبيق مختلفة بينما مواصفة اللقطة والشرح ينصان على عدم وجود إطار | لا تغيير DB/API/RLS/الصلاحيات؛ لا يُضاف إطار هاتف اصطناعي؛ يلزم مصدر لقطة متوافق قبل إغلاق المقارنة | جزئي — وظيفي ناجح، بصري محجوب بتعارض الأصل |
| 005 | `screens/005_password_setup.png` + `explanations/005_password_setup.md` | إنشاء وتأكيد كلمة المرور — عميل جديد — هاتف حقيقي — مرجع 6.3 | `apps/mobile_flutter/lib/features/customer/customer_account.dart`; `apps/mobile_flutter/test/task005_password_setup_test.dart`; `apps/mobile_flutter/test/visual_reference/task005_password_setup_reference_360x780.png` | التسجيل يستدعي `repository.register(name, email, password)` بعد تحقق الاسم/البريد/كلمة المرور والتأكيد؛ تسجيل الدخول القائم بقي passwordless عبر `requestEmailOtp(email)`، دون API جديد | وضع التسجيل يعرض العنوان والوصف العربيين، الاسم والبريد وكلمة المرور والتأكيد، مؤشرات ضعيفة/متوسطة/قوية، ورسالة «حفظ ومتابعة». يمنع التسجيل الضعيف أو غير المتطابق، ويُبقي زر الحفظ معطلاً أثناء الطلب؛ نجاح Demo يحدّث الجلسة عبر المستودع | حالة ضعيفة، حالة متوسطة، حالة قوية، عدم تطابق، حاجز كلمة المرور الضعيفة، نجاح `register` في Demo، واستمرار تسجيل الدخول القائم بلا كلمة مرور | `flutter test test/task005_password_setup_test.dart --reporter expanded`: 3 اختبارات وظيفية نجحت؛ اختبار golden فشل بنسبة اختلاف 100% (`280800px`) وسُجل كحجب بصري. `flutter analyze --no-pub`: No issues found | الأصل 1440×3120 عبارة عن لوحة علوية واسعة ذات بطاقة/وسوم وأزرار «حفظ/إلغاء» ومساحة بيضاء، بينما العقد يصف لقطة هاتف RTL تفاعلية؛ المرجع المصغر 360×780 أُنشئ حتميًا من الأصل، ولم يُستبدل بالـactual أو baseline مصطنع | ينطبق `GAP-003` و`GAP-009`: لا تغيير DB/API/RLS/الصلاحيات، ولا تُضاف أفعال حفظ/إلغاء الشكلية المتعارضة؛ يلزم حسم مرجع الهاتف/حل golden harness قبل إغلاق القبول البصري | جزئي — وظيفي ناجح، بصري محجوب بتعارض الأصل/golden |
| 006 | `screens/006_app_shell.png` + `explanations/006_app_shell.md` | الغلاف العام وشريط التنقل السفلي — زائر / عميل — هاتف حقيقي — مرجع 4 | `apps/mobile_flutter/lib/app/assal_app.dart`; `apps/mobile_flutter/lib/features/customer/customer_favorites.dart`; `apps/mobile_flutter/lib/core/assal_widgets.dart`; `apps/mobile_flutter/test/task006_app_shell_test.dart`; `apps/mobile_flutter/test/navigation_test.dart`; `apps/mobile_flutter/test/visual_reference/task006_app_shell_reference_360x780.png` | لا مصدر جديد ولا تغيير API؛ الصفحات تستخدم `AssalRepository` القائم، و`AssalHomeShell` يحتفظ بصفحات الوجهات لمنع إعادة إنشاء Futures أثناء التنقل | غلاف الهاتف يعرض ست وجهات عربية فعلية: «الرئيسية»، «البحث»، «المحفوظات»، «المتابعات»، «الملف الشخصي»، «الإشعارات». المحفوظات والمتابعات تستخدمان كيان Favorites الموحد مع `initialTab`، والغلاف الواسع يعرض NavigationRail؛ أصلحت مكونات التحميل ورؤوس الأقسام overflow responsive دون تغيير الأفعال | تنقل الضيف بين الوجهات الست، حالة دخول المحفوظات/الإشعارات، فصل تبويب المحفوظات عن المتابعات، NavigationRail بعرض منطقي 900px، RTL، ومنع pumpAndSettle من تعليق الاختبار بسبب البنر المتحرك | `flutter test test/task006_app_shell_test.dart --reporter expanded`: اختبارا السلوك/الـrail نجحا؛ golden فشل بنسبة 100% (`280796px`) وسُجل كحجب بصري. `flutter test test/navigation_test.dart --reporter expanded`: نجح. `flutter analyze --no-pub`: No issues found | المرجع المصغر 360×780 مشتق حتميًا من PNG 1440×3120، لكن المصدر لوحة عريضة فيها بطاقة وشرائح وزرا «حفظ/إلغاء» ومساحة فارغة، بينما عقد 006 يطلب غلاف هاتف حقيقي؛ لم يُصنع baseline من actual | ينطبق `GAP-003` و`GAP-009`: لا تغيير DB/API/RLS/الصلاحيات؛ يلزم حسم مرجع الهاتف/آلية golden قبل إغلاق القبول البصري. عولج overflow الذي كشفه الاختبار في `AssalGlassLoading` و`SectionHeader` بإتاحة الالتفاف/الاختصار بدل القص | جزئي — وظيفي ناجح، بصري محجوب بتعارض الأصل/golden |
| 007 | `screens/007_home.png` + `explanations/007_home.md` | الصفحة الرئيسية والاستكشاف — زائر / عميل — هاتف حقيقي — مرجع 7 | `apps/mobile_flutter/lib/features/customer/customer_discovery.dart`; `apps/mobile_flutter/test/task007_home_test.dart`; `apps/mobile_flutter/test/visual_reference/task007_home_reference_360x640.png` | لا مصدر جديد ولا تغيير API؛ HomeScreen يستخدم Futures القائمة من `AssalRepository` للبنرات والتصنيفات والمنتجات والمتاجر والإشعارات | طُبقت نصوص العقد الأساسية: حقل «ابحث عن عسل أو متجر»، Hero fallback بعنوان «طبيعة أصيلة .. عسل أصيل» ووصفه وCTA «اكتشف الآن»، «استكشف حسب التصنيف»، «منتجات مختارة»، «الأكثر مشاهدة»، «وصل حديثًا»، «الأعلى تقييمًا»، و«متاجر عسلكم». بقيت الروابط إلى البحث/المتاجر، إعادة المحاولة وحالات loading/empty/error، وصورة المنتج في `ProductCard` بنسبة 1:1 | اختبار CTA البنر والبحث، ظهور جميع rails بعد التمرير، منع InkWell بلا Material في التصنيف، منع overflow لبطاقات rail ذات العرض 168، وحالات DemoData | `flutter test test/task007_home_test.dart --reporter expanded`: 2 اختبارين وظيفيين نجحا؛ golden فشل بنسبة 99.84% (`230029px`) فقط. `flutter analyze --no-pub`: No issues found (8.0s) | المرجع المصغر 360×640 مشتق حتميًا من PNG 1440×2560؛ الأصل يعرض لقطة مكتملة بترتيب/صور/بطاقات مختلفة، بينما Demo fixture محدود وبنر بلا صورة، لذلك لا تُسجل مطابقة بصرية ولا يُصنع baseline من actual | لا تغيير DB/API/RLS/الصلاحيات. أُصلح خلل UI مثبت: `CategoryTile` صار داخل `Material`، و`ProductRail` رفع ارتفاع الحاوية لاستيعاب البطاقة دون قص؛ لا بيانات إنتاجية مزيفة | جزئي — وظيفي ناجح، بصري محجوب بتعارض الأصل/بيانات العرض؛ عولجتا GAP-014 وGAP-015 داخل المهمة |
| 008 | `screens/008_product_card.png` + `explanations/008_product_card.md` | بطاقة المنتج الموحدة — كل الأدوار — أصل 1:1 — مرجع 7.2 | `apps/mobile_flutter/lib/core/assal_widgets.dart`; `apps/mobile_flutter/test/task008_product_card_test.dart`; `apps/mobile_flutter/test/visual_reference/task008_product_card_reference_360x360.png`; `apps/mobile_flutter/test/product_card_widget_test.dart` | لا مصدر جديد ولا تغيير API؛ أضيفت معاملات UI اختيارية `store` و`onFavorite` إلى ProductCard مع بقاء الاستدعاءات السابقة صالحة، وتستخدم الحقول الموجودة في `AssalProductSummary` و`AssalStoreSummary` | البطاقة الموحدة تحافظ على `AspectRatio(1)` وتعرض شارة التوفر، التصنيف، اسم المنتج، التقييم الرقمي وعدد التقييمات، السعر بفواصل الآلاف، الوزن، ومعلومات المتجر عند توفيرها. زر «حفظ المنتج» يظهر فقط مع callback حقيقي، وعلامة التحقق تظهر فقط من `store.isVerified` المصدرية؛ لا Pro أو إجراء شكلي | حقول المرجع «عسل السدر الجبلي الفاخر»، «عسل السدر»، «15,000 ريال يمني»، 4.8، 128 تقييم، «متوفر»، 500 جم، «مناحل جبال اليمن»، «إب، اليمن»؛ تفاعل الحفظ، عرض rail بعرض 168 دون overflow، واختبار regression السابق | `flutter test test/task008_product_card_test.dart --reporter expanded`: 2 اختبارين وظيفيين نجحا، وgolden فشل بنسبة 100% (`129600px`) فقط. `flutter test test/product_card_widget_test.dart --reporter expanded`: نجح. `flutter analyze --no-pub`: No issues found (8.7s) | المرجع 1920×1920 صُغّر حتميًا إلى 360×360، لكن actual card يعتمد fallback أيقونة بلا صورة شبكة ويمتد رأسيًا بينما PNG غني بصورة وتخطيط مختلف؛ لم تُصنع مطابقة أو baseline من actual | لا تغيير DB/API/RLS/الصلاحيات. أُصلح نقص UI مثبت بإظهار الوزن/التوفر/المتجر والحفظ الاختياري، وتنسيق السعر؛ علامة التحقق مقيدة بحقل العقد ولا تُعرض كـPro. ينطبق `GAP-009` على الحجب البصري | جزئي — وظيفي ناجح، بصري محجوب بتعارض الأصل/renderer، وعولج GAP-016 داخل المهمة |
| 009 | `screens/009_store_card.png` + `explanations/009_store_card.md` | بطاقة المتجر الموحدة — كل الأدوار — أصل 4:5 — مرجع 7.3 | `apps/mobile_flutter/lib/core/assal_widgets.dart`; `apps/mobile_flutter/test/task009_store_card_test.dart`; `apps/mobile_flutter/test/visual_reference/task009_store_card_reference_360x450.png` | لا مصدر جديد ولا تغيير API؛ StoreCard يعيد استخدام `AssalStoreSummary` ويدعم معاملات UI الاختيارية القائمة، ومنها `onAction` لإزالة المتابعة و`productCount` | بطاقة 4:5 تعرض غلاف المتجر، الشعار/البديل، الاسم، الموقع، إحصاءات المتابعين والتقييم والمنتجات، وزر «عرض المتجر» المرتبط بـ`onTap` الحقيقي. عدد المتابعين يختصر 2300 إلى 2.3K من قيمة العقد، ولا تُعرض علامة Pro/توثيق غير مطلوبة | حقول المرجع «مناحل جبال اليمن»، «إب، اليمن»، «2.3K متابع»، «4.8 تقييم»، «156 منتج»، زر العرض، وإجراء إزالة المتابعة الاختياري؛ اختبار العرض بعرض 220px بلا overflow | `flutter test test/task009_store_card_test.dart --reporter expanded`: 2 اختبارين وظيفيين نجحا؛ golden فشل بنسبة 100% (`162000px`) فقط. `flutter analyze --no-pub`: No issues found (10.4s) | المرجع 1664×2080 صُغّر حتميًا إلى 360×450؛ actual يستخدم fallback للشعار/الغلاف داخل harness بلا صور شبكة، لذلك لا تُسجل مطابقة بصرية ولا يُصنع baseline من actual | لا تغيير DB/API/RLS/الصلاحيات؛ بقيت الأفعال callbacks حقيقية، ولم تُضاف بيانات متجر ثابتة داخل المكوّن | جزئي — وظيفي ناجح، بصري محجوب بتعارض الأصل/renderer؛ عولج GAP-017 داخل المهمة |
| 010 | `screens/010_search.png` + `explanations/010_search.md` | البحث ونتائج المنتجات والمتاجر — زائر / عميل — هاتف حقيقي — مرجع 9.1 | `apps/mobile_flutter/lib/features/customer/customer_discovery.dart`؛ `apps/mobile_flutter/test/task010_search_test.dart`؛ `apps/mobile_flutter/test/visual_reference/task010_search_reference_360x640.png` | لا API أو مصدر جديد؛ `SearchScreen` يستخدم `AssalProductQuery` الموجود مع البحث والترتيب والفلاتر، ويستدعي `listStores()` الموجود لعرض المتاجر. | وُحّد نص حقل البحث إلى «ابحث عن منتج أو متجر» وعنوان النتائج إلى «نتائج البحث». بقيت إجراءات البحث الحقيقي عند الإرسال، PopupMenu للترتيب، Bottom Sheet للفلاتر، حالات التحميل/الخطأ/الفراغ وإعادة المحاولة، وانتقال بطاقات المنتج والمتجر إلى التفاصيل عبر callbacks حقيقية. | اختبار البحث باستخدام «سدر» والفرز إلى «الأحدث» نجح، واختبار فتح الفلاتر وإظهار حقول المحافظة/المديرية/القسم/التصنيف والنوع/المتاجر الموثقة والتطبيق نجح. لم يُثبت بحث نصي مستقل للمتاجر لأن عقد `AssalRepository` الحالي يستدعي `listStores()` بلا وسيط بحث؛ لا أُنشئ API بديلًا داخل هذه المهمة. | `flutter test --no-pub test/task010_search_test.dart --reporter expanded`: نجح الاختباران الوظيفيان، وفشل اختبار golden فقط بنسبة `99.99%` (`230386px`) كما هو متوقع ومُسجل. `flutter test --no-pub test/search_filter_widget_test.dart test/search_location_cascade_widget_test.dart --reporter expanded`: نجح الاختباران. `flutter analyze --no-pub`: No issues found. | المرجع الأصلي 1440×2560، والمرجع المنطقي 360×640 مشتق حتميًا. المقارنة لم تُسجل نجاحًا لأن actual يعتمد renderer/أصول Demo مختلفة عن اللوحة المرجعية، مع غياب صور الشبكة وتعارض تخطيط المصدر؛ لم يُستبدل المرجع بـactual ولم يُصنع baseline مصطنع. | لا تغيير DB/API/RLS/الصلاحيات. ينطبق `GAP-009` على حجب golden؛ عرض المتاجر موجود من المصدر الحالي لكن free-text filtering للمتاجر غير مثبت بعقد المستودع. | جزئي — وظيفي ناجح، بصري محجوب بتعارض الأصل/renderer |
| 011 | `screens/011_search_filters.png` + `explanations/011_search_filters.md` | نافذة فلاتر البحث — زائر / عميل — هاتف حقيقي مع Bottom Sheet — مرجع 9.2 | `apps/mobile_flutter/lib/features/customer/customer_discovery.dart`؛ `apps/mobile_flutter/test/task011_search_filters_test.dart`؛ `apps/mobile_flutter/test/search_filter_widget_test.dart`؛ `apps/mobile_flutter/test/visual_reference/task011_search_filters_reference_360x640.png` | لا DB/API جديد؛ أُعيد استخدام `AssalProductQuery` و`AssalSort` و`YemenLocationReference` و`listStores()` الحالية، مع تمرير `storeId` الموجود إلى استعلام المنتجات. | أُعيد تنظيم Bottom Sheet بخلفية كريمية وحواف علوية ناعمة ومقبض سحب ورأس «الفلاتر» مع إغلاق حقيقي. أضيفت عناوين أقسام «الموقع»، «السعر»، «نوع المنتج»، «التصنيف»، «الجودة»، «الوزن أو الحجم»، «حالة التوفر» و«المتجر»، مع توسيع حقول الاختيار على الهاتف. أضيف زر «إلغاء» ومسح الكل الحقيقي، وبقي «تطبيق الفلاتر» يغلق النافذة ويعيد البحث. | حقول المحافظة/المديرية مع cascade، القسم والتصنيف الفرعي، نوع المنتج، الجودة، التحقق، الأصل والمعالجة والتعبئة والتوفر، نطاق السعر والتقييم، اختيار المتجر، الإغلاق دون تطبيق، ومسح الفلاتر. عرض الوزن/الحجم وعملة السعر للقراءة فقط لأن `AssalProductQuery` لا يحتوي مرشحي وزن أو عملة؛ سُجل ذلك في `GAP-018` بدل اختراع API. | `flutter test --no-pub test/task011_search_filters_test.dart --reporter expanded`: 3 اختبارات وظيفية نجحت، وفشل golden فقط بنسبة 100% (`230400px`). `flutter test --no-pub test/search_filter_widget_test.dart test/search_location_cascade_widget_test.dart --reporter expanded`: نجح الاختباران. `flutter analyze --no-pub`: No issues found. | المرجع الأصلي 1440×2560، والمرجع المنطقي 360×640 مشتق حتميًا. فشلت المقارنة البصرية بنسبة 100% (`230400px`) بسبب اختلاف renderer/الأصول وتخطيط اللوحة المرجعية عن actual؛ لم يُعتمد actual كbaseline ولم تُصنع نتيجة نجاح. | لا تغيير DB/API/RLS/الصلاحيات. `GAP-009` يحجب القبول البصري، و`GAP-018` يوثق عدم وجود حقلي وزن/عملة قابلين للتمرير إلى الاستعلام. store filtering المدعوم هنا يخص المنتجات عبر `storeId`؛ `listStores()` نفسه بلا نص بحث في العقد الحالي. | جزئي — وظيفي ناجح، بصري محجوب بتعارض الأصل/renderer |
| 012 | `screens/012_search_sort.png` + `explanations/012_search_sort.md` | نافذة ترتيب النتائج — زائر / عميل — هاتف حقيقي مع Bottom Sheet — مرجع 9.3 | `apps/mobile_flutter/lib/features/customer/customer_discovery.dart`؛ `apps/mobile_flutter/test/task012_search_sort_test.dart`؛ `apps/mobile_flutter/test/task010_search_test.dart`؛ `apps/mobile_flutter/test/visual_reference/task012_search_sort_reference_360x780.png` | لا DB/API جديد؛ أُعيد استخدام `AssalSort` الحالي و`AssalProductQuery` و`listProducts()`. لم تُضف قيم enum جديدة أو مسارات ترتيب غير مدعومة. | استُبدل PopupMenu المختصر بنافذة Bottom Sheet RTL بخلفية كريمية وحواف ناعمة ورأس «ترتيب النتائج» وإغلاق حقيقي. تعرض النافذة الخيارات المصدرية «ترتيب النتائج»، «الأكثر مشاهدة»، «الأحدث» و«الأعلى تقييمًا» عبر RadioListTile، وتوضح بصراحة أن الفرز بالسعر أو الأقرب حسب المنطقة غير متاح من المصدر الحالي. زر «تطبيق» يعيد قيمة `AssalSort` ويعيد تحميل النتائج، والزر يعرض الاختيار الحالي بصورة مختصرة على الهاتف. | اختبار عرض الخيارات الأربعة، النص التوضيحي للخيارات غير المدعومة، تطبيق «الأحدث» وبقاء المنتج من المصدر، والإغلاق دون تغيير. جرى تحديث regression TASK 010 من PopupMenu إلى Bottom Sheet دون تغيير عقد البيانات. | `flutter test --no-pub test/task012_search_sort_test.dart --reporter expanded`: 3 اختبارات وظيفية نجحت، وفشل golden فقط بنسبة 100% (`280800px`). `flutter test --no-pub test/task010_search_test.dart --reporter expanded`: نجح اختباران وظيفيان، وفشل golden بنسبة 99.99% (`230386px`). `flutter test --no-pub test/search_filter_widget_test.dart test/search_location_cascade_widget_test.dart --reporter expanded`: نجح الاختباران. `flutter analyze --no-pub`: No issues found. | المرجع الأصلي 1440×3120، والمرجع المنطقي 360×780 مشتق حتميًا. فشلت المقارنة البصرية بنسبة 100% (`280800px`) بسبب اختلاف قالب PNG/renderer والأصول عن actual Bottom Sheet؛ لم يُعتمد actual كbaseline ولم تُصنع مطابقة مصطنعة. | لا تغيير DB/API/RLS/الصلاحيات. `GAP-009` يحجب القبول البصري، و`GAP-019` يوثق أن السعر/الأقرب حسب المنطقة غير موجودين في `AssalSort`/عقد الاستعلام؛ عُرضت رسالة واضحة بدل أزرار ميتة. | جزئي — وظيفي ناجح، بصري محجوب بتعارض الأصل/renderer |
| 013 | `screens/013_categories.png` + `explanations/013_categories.md` | التصنيفات والأنواع — زائر / عميل — هاتف حقيقي — مرجع 9.4 | `apps/mobile_flutter/lib/features/customer/customer_discovery.dart`؛ `apps/mobile_flutter/test/task013_categories_test.dart`؛ `apps/mobile_flutter/test/visual_reference/task013_categories_reference_360x780.png` | لا مصدر جديد ولا تغيير DB/API؛ `CategoriesScreen` يستخدم `listCategories()` و`AssalCategorySummary` و`ProductType` الحالية. | أُضيفت شاشة Stateful بحقل بحث وشرائح الأنواع وتحديث/إعادة محاولة وسحب للتحديث؛ الضغط على التصنيف ينتقل إلى `SearchScreen(initialCategoryId)`، وصُحح `_reload` ليستخدم callback `setState` متزامنًا. | تحميل/فراغ/خطأ/إعادة محاولة، بحث وتصفية نوعية، مصدر repository فعلي وانتقال حقيقي؛ لا بيانات إضافية أو أزرار ميتة. | اختبار TASK 013: 3 وظيفية ناجحة؛ regressions البحث والفلاتر والموقع ناجحة وظيفيًا؛ `flutter analyze --no-pub`: No issues found. | فشل golden بنسبة 100.00% (`280800px`) بسبب تعارض الأصل العريض/renderer مع viewport الهاتف، ولم يُستخدم actual كـ baseline. | لا تغيير DB/API/RLS/الصلاحيات؛ القبول البصري محجوب موثقًا. | جزئي — وظيفي ناجح، بصري محجوب |
| 014 | `screens/014_stores_list.png` + `explanations/014_stores_list.md` | قائمة المتاجر — زائر / عميل — هاتف حقيقي — مرجع 10 | `apps/mobile_flutter/lib/features/customer/customer_discovery.dart`؛ `apps/mobile_flutter/test/task014_stores_list_test.dart`؛ `apps/mobile_flutter/test/visual_reference/task014_stores_list_reference_360x780.png` | لا DB/API/RLS جديد؛ تستخدم الشاشة `listStores({regionId})` و`AssalStoreSummary` و`YemenLocationReference` الموجودة، مع تصفية محلية للاسم/المنطقة والتوثيق. | أُعيد بناء المسار برأس «قائمة المتاجر»، حقل بحث، شريط «متصل/تحديث»، بطاقة إبراز مصدرية، شرائح المتاجر والبحث والفلاتر والمنطقة والترتيب، بطاقة تفاصيل المصدر، وأزرار حفظ/إلغاء داخل Bottom Sheet الفلاتر. بطاقة المتجر تنتقل إلى `StoreProfileScreen` الحقيقي؛ الترتيب يعرض رسالة صريحة لغياب عقد ترتيب متاجر مستقل. | تحميل/فراغ/خطأ/إعادة محاولة، سحب للتحديث، بحث الاسم/المنطقة، فلتر المنطقة والتوثيق، Bottom Sheet فوري حتى عند تعذر مرجع المناطق، وإعادة ضبط حقيقية؛ لا بيانات متجر ثابتة أو أفعال ميتة. | `flutter test --no-pub test/task014_stores_list_test.dart --reporter expanded`: 3 اختبارات وظيفية ناجحة؛ golden فشل فقط بنسبة 100.00% (`280799px`). regressions `task009_store_card_test.dart` و`task010_search_test.dart`: الوظائف ناجحة وgolden السابقان محجوبان؛ `flutter analyze --no-pub`: No issues found. | المرجع الأصلي 1440×3120، والمرجع المنطقي 360×780 مشتق حتميًا. فشل golden بسبب تعارض قالب PNG/renderer/بيانات الصور عن actual؛ لم يُعتمد actual كـ baseline. | لا تغيير DB/API/RLS/الصلاحيات؛ fallback مرجع المناطق يعرض سبب التعذر ويُبقي فلتر التوثيق قابلًا للاستخدام، ولا توجد فجوة عقد جديدة مثبتة. | جزئي — وظيفي ناجح، بصري محجوب |
| 015 | `screens/015_product_detail_overview.png` + `explanations/015_product_detail_overview.md` | تفاصيل المنتج: المعرض والقرار والمعلومات — عميل — هاتف حقيقي — مرجع 11.1–11.3 | `apps/mobile_flutter/lib/features/customer/customer_catalog.dart`؛ `apps/mobile_flutter/test/task015_product_detail_overview_test.dart`؛ `apps/mobile_flutter/test/visual_reference/task015_product_detail_overview_reference_360x640.png` | لا DB/API/RLS جديد؛ تستخدم الشاشة `getProduct()` و`getStore()` وحقول `AssalProductSummary`/`AssalStoreSummary` الحالية، مع callbacks التفاعل الموجودة. | أُعيد تنظيم المسار داخل `NestedScrollView` لتفاصيل منتج قابلة للتمرير: معرض 1:1 مع مؤشرات وحفظ فعلي، شارة التصنيف والعنوان والتقييمات، ملخص المتجر، بطاقة «خيارات التوفر والاستلام» بسعر/حالة/توصيل/استلام وزر «اسأل عن التوفر»، ثم تبويبات «معلومات المنتج» و«التقييمات والتفاعل» و«منتجات مشابهة». أضيفت حقول المصدر مثل المكونات، وحُصنت حالة الخطأ بزر إعادة محاولة متزامن؛ لا أفعال ميتة. | تحميل ونجاح وفراغ/خطأ وإعادة محاولة، معرض صورة 1:1، حفظ ومشاركة، معلومات المصدر والجودة والمكونات، تبويبات قابلة للتمرير، واستمرار زر سؤال التوفر. عولج overflow المثبت بتحويل التخطيط إلى `NestedScrollView`، ووُحّدت شارة التوفر لمنع التكرار. | `flutter test --no-pub test/task015_product_detail_overview_test.dart --reporter expanded`: 3 اختبارات وظيفية ناجحة؛ golden فشل فقط بنسبة 99.98% (`230357px`). regression `product_detail_ux_test.dart` نجح وظيفيًا؛ `flutter analyze --no-pub`: No issues found. | المرجع الأصلي 1440×2560، والمرجع المنطقي 360×640 مشتق حتميًا. فشل golden بسبب اختلاف PNG/renderer/أصول الصور وبيانات Demo عن actual؛ لم يُستخدم actual كـ baseline. | لا تغيير DB/API/RLS/الصلاحيات؛ لا فجوة عقد جديدة مثبتة، والحجب البصري يستمر ضمن فجوة golden القائمة. | جزئي — وظيفي ناجح، بصري محجوب |
| 016 | `screens/016_product_detail_social.png` + `explanations/016_product_detail_social.md` | التقييمات والتعليقات والتفاعل — عميل — هاتف حقيقي — مرجع 11.4 | `apps/mobile_flutter/lib/features/customer/customer_catalog.dart`؛ `apps/mobile_flutter/lib/features/customer/customer_social.dart`؛ `apps/mobile_flutter/test/task016_product_detail_social_test.dart`؛ `apps/mobile_flutter/test/visual_reference/task016_product_detail_social_reference_360x640.png` | لا DB/API/RLS جديد؛ تستخدم التقييمات والتعليقات وعمليات الإعجاب والحفظ الحالية في `AssalRepository` وعقود `AssalReviewSummary`/`AssalCommentSummary`. | دُمج تبويب التفاعل مع ملخص تقييم مصدرّي، أزرار «إعجاب/حفظ» حقيقية، قائمة المراجعات والتعليقات، محرر تعليق مع إرسال فعلي ومزامنة محلية متفائلة، ومحرر تقييم بتقييم/نص وحاجز نشر حتى وجود النص. تعالج الواجهة حالة «قيد المراجعة؛ سيظهر للآخرين بعد الاعتماد»، وتبقى أفعال الإرسال معطلة أثناء الحفظ. ثُبت تبويب التفاعل داخل `SliverPersistentHeader`، وعولج overflow عنوان المراجعة بـ`Wrap`، وأصلح lifecycle محرر التقييم بفصل controller داخل Dialog مستقل. | تحميل/فراغ/خطأ/إعادة محاولة للمراجعات والتعليقات، حالات guest عبر نافذة الدخول القائمة، تفاعل إعجاب وحفظ، إرسال تعليق وتقييم، نتيجة متفائلة ظاهرة، وحالة pending moderation؛ لا بيانات اجتماعية ثابتة أو أزرار ميتة. | `flutter test --no-pub test/task016_product_detail_social_test.dart --reporter expanded`: 3 اختبارات وظيفية ناجحة؛ golden فشل فقط بنسبة 99.96% (`230314px`). regressions `task097_social_accessibility_test.dart` و`product_detail_ux_test.dart`: نجحت؛ `flutter analyze --no-pub`: No issues found. | المرجع الأصلي 1440×2560، والمرجع المنطقي 360×640 مشتق حتميًا. فشل golden بسبب اختلاف قالب PNG/renderer/صور البيانات عن actual، مع تسجيل overflow اختباري ثم إصلاحه؛ لم يُعتمد actual كـ baseline. | لا تغيير DB/API/RLS/الصلاحيات؛ لا فجوة عقد جديدة مثبتة، والحجب البصري مستمر ضمن فجوة golden القائمة. | جزئي — وظيفي ناجح، بصري محجوب |
| 017 | `screens/017_review_sheet.png` + `explanations/017_review_sheet.md` | نافذة كتابة التقييم — عميل — هاتف حقيقي مع نافذة داخلية — مرجع 11.5 | `apps/mobile_flutter/lib/features/customer/customer_social.dart`؛ `apps/mobile_flutter/test/task017_review_sheet_test.dart`؛ `apps/mobile_flutter/test/visual_reference/task017_review_sheet_reference_360x780.png` | لا DB/API/RLS جديد؛ تستخدم النافذة `AssalReviewDraft` و`createReview()` وعقد الجلسة الحالية عبر `ReviewsSection`. | أُعيد تصميم نافذة `مراجعتك` بخلفية كريمية وحواف عسلكم وأيقونة وهوية واضحة، واختيار نجوم 1–5 عبر ChoiceChip، حقل نص متعدد الأسطر مع hint «شارك ما يفيد الآخرين»، وأفعالي «إلغاء» و«نشر» حقيقيين. يبقى النشر معطلاً حتى وجود نص، ثم تعود النتيجة للمستودع وتظهر حالة الاعتماد الحالية. | عرض النافذة، اختيار التقييم، حاجز النص، الإلغاء دون إرسال، الإرسال مع تعطيل الزر أثناء الطلب، ورسالة pending الموجودة؛ controller داخل Dialog Stateful مستقل لتفادي خلل lifecycle. | `flutter test --no-pub test/task017_review_sheet_test.dart --reporter expanded`: 3 اختبارات وظيفية ناجحة؛ golden فشل فقط بنسبة 100.00% (`280800px`). regressions `task097_social_accessibility_test.dart` وTASK 016 الوظيفية: نجحت؛ `flutter analyze --no-pub`: No issues found. | المرجع الأصلي 1440×3120، والمرجع المنطقي 360×780 مشتق حتميًا. فشل golden بسبب اختلاف قالب PNG/renderer عن actual؛ لم يُعتمد actual كـ baseline. | لا تغيير DB/API/RLS/الصلاحيات؛ لا فجوة عقد جديدة مثبتة، والحجب البصري مستمر ضمن فجوة golden القائمة. | جزئي — وظيفي ناجح، بصري محجوب |
| 018 | `screens/018_similar_products.png` + `explanations/018_similar_products.md` | المنتجات المشابهة — عميل — هاتف حقيقي — مرجع 11.6 | `apps/mobile_flutter/lib/features/customer/customer_catalog.dart`; `apps/mobile_flutter/lib/core/assal_widgets.dart`; `apps/mobile_flutter/test/task018_similar_products_test.dart`; `apps/mobile_flutter/test/visual_reference/task018_similar_products_reference_360x780.png` | لا DB/API/RLS جديد؛ يستخدم المسار `widget.repository.listProducts(query: AssalProductQuery(subcategoryId: product.taxonomyId))` من العقد القائم. معيار التشابه المثبت هو `taxonomyId`/`subcategoryId`؛ المنتج بلا taxonomy يعرض حالة مصدرية صريحة بدل اختراع معيار أو بيانات | أضيف عنوان «المنتجات المشابهة»، شبكة `ProductCard` بمرشح يستبعد المنتج الحالي، وانتقال حقيقي إلى `ProductDetailScreen` عند الضغط. لا أضيف أزرار عودة/بحث شكلية لأن المسار الحالي يوفر التنقل القائم ولا يثبت عقدًا مستقلًا لهما | loading، نجاح، empty عند عدم وجود عناصر بعد الاستبعاد، error، retry يعيد إنشاء Future، وغياب taxonomy برسالة واضحة؛ صور البطاقات بقيت 1:1. حُصّن `InfoChip` من overflow، وضُبط ارتفاع الشبكة، وطابق ارتفاع التبويب المثبت TabBar لمنع SliverGeometry regression | `flutter test --no-pub test/task018_similar_products_test.dart --reporter expanded`: 4 اختبارات وظيفية نجحت؛ golden فشل فقط بنسبة 100.00% (`280797px`) مقابل المرجع المصغّر 360×780. regressions TASK 015 الوظيفية، TASK 008 الوظيفية، TASK 016 الوظيفية، TASK 017 الوظيفية نجحت؛ `flutter analyze --no-pub`: No issues found | PNG الأصلي 1440×3120 وصُغّر deterministic إلى `360×780` كمرجع مستقل. فشلت المقارنة 100.00% (`280797px`) لأن قالب PNG/renderer/البيانات تختلف عن actual؛ لم يُعتمد actual كbaseline ولم تُسجل مطابقة بصرية | لا تغيير DB/API/RLS/الصلاحيات ولا GAP جديد؛ يستمر حجب القبول البصري ضمن فجوة golden القائمة، مع عدم اختراع معيار تشابه خارج `taxonomyId` | جزئي — وظيفي ناجح، بصري محجوب بتعارض الأصل/renderer |
| 019 | `screens/019_store_profile_products.png` + `explanations/019_store_profile_products.md` | صفحة المتجر: المنتجات — زائر / عميل — هاتف حقيقي — مرجع 12.1–12.3 | `apps/mobile_flutter/lib/features/customer/customer_catalog.dart`; `apps/mobile_flutter/lib/core/assal_widgets.dart`; `apps/mobile_flutter/test/task019_store_profile_products_test.dart`; `apps/mobile_flutter/test/visual_reference/task019_store_profile_products_reference_360x640.png` | لا DB/API/RLS جديد؛ يستخدم `getStore(widget.storeId)` و`listProducts(query: AssalProductQuery(storeId: widget.storeId))` وعقود `AssalStoreSummary`/`AssalProductSummary` الحالية | صفحة متجر بثلاثة تبويبات حقيقية: «المنتجات»، «معلومات المتجر»، «التواصل». تبويب المنتجات يعرض شبكة `ProductCard` مربعة 1:1، حفظًا مربوطًا بـ`toggleFavorite`، والضغط على البطاقة ينتقل إلى `ProductDetailScreen`. زر المتابعة وإحصاء المتابعين وإجراءات المتجر القائمة بقيت مرتبطة بالمستودع؛ لا أزرار شكلية | loading، نجاح، empty برسالة عربية وزر «العودة إلى المتاجر»، error/retry للمتجر والمنتجات، حالة جلسة الحفظ، وشبكة منتجات مصدرية. عولجت قابلية تمرير حالات error/empty داخل TabBarView، وضُبطت callbacks `setState` المتزامنة | `flutter test --no-pub test/task019_store_profile_products_test.dart --reporter expanded`: 5 اختبارات وظيفية نجحت؛ golden فشل فقط بنسبة 99.99% (`230372px`). regressions TASK 015 وTASK 008 الوظيفية نجحت؛ `flutter analyze --no-pub`: No issues found | PNG الأصلي 1440×2560 وصُغّر deterministic إلى `360×640`. فشلت المقارنة بنسبة 99.99% (`230372px`) بسبب اختلاف قالب PNG/renderer/بيانات Demo عن actual؛ لم يُعتمد actual كbaseline | لا تغيير DB/API/RLS/الصلاحيات ولا GAP جديد؛ يستمر حجب القبول البصري ضمن فجوة golden القائمة | جزئي — وظيفي ناجح، بصري محجوب بتعارض الأصل/renderer |
| 020 | `screens/020_store_profile_info.png` + `explanations/020_store_profile_info.md` | صفحة المتجر: المعلومات — زائر / عميل — هاتف حقيقي — مرجع 12.4 | `apps/mobile_flutter/lib/features/customer/customer_catalog.dart`; `apps/mobile_flutter/test/task020_store_profile_info_test.dart`; `apps/mobile_flutter/test/visual_reference/task020_store_profile_info_reference_360x780.png` | لا DB/API/RLS جديد؛ يعرض التبويب حقول `AssalStoreSummary` من `getStore(widget.storeId)`: الوصف/النبذة، المنطقة، المعرض، سنوات الخبرة، التخصصات، التوثيقات، التوصيل ونقاط الاستلام | تبويب «معلومات المتجر» حقيقي داخل `StoreProfileScreen`، يعرض المعرض المصدرّي و«عن المتجر» و«الموقع والتخصصات» و«طرق التوصيل» و«نقاط الاستلام». لا أضيف أفعال حفظ/إلغاء الشكلية من PNG لأن هذا المسار عميل/زائر ولا يوجد callback حفظ قائم | loading/نجاح عبر مسار المتجر، empty guidance لكل حقل ناقص، error مع «إعادة المحاولة» مرتبطة بإعادة جلب المتجر، نصوص RTL، وبيانات سنوات الخبرة والتوصيل والاستلام من العقد فقط | `flutter test --no-pub test/task020_store_profile_info_test.dart --reporter expanded`: 3 اختبارات وظيفية نجحت؛ golden فشل فقط بنسبة 100.00% (`280800px`). regression TASK 019 وTASK 015/008 المرتبطان بالمسار نجحا وظيفيًا؛ `flutter analyze --no-pub`: No issues found | PNG الأصلي 1440×3120 وصُغّر deterministic إلى `360×780`. فشلت المقارنة بنسبة 100.00% (`280800px`) بسبب اختلاف قالب PNG/renderer/بيانات Demo عن actual؛ لم يُعتمد actual كbaseline | لا تغيير DB/API/RLS/الصلاحيات ولا GAP جديد؛ يستمر حجب القبول البصري ضمن فجوة golden القائمة | جزئي — وظيفي ناجح، بصري محجوب بتعارض الأصل/renderer |
| 021 | `screens/021_store_profile_contact.png` + `explanations/021_store_profile_contact.md` | صفحة المتجر: التواصل والتوصيل والاستلام — زائر / عميل — هاتف حقيقي — مرجع 12.5 | `apps/mobile_flutter/lib/features/customer/customer_catalog.dart`; `apps/mobile_flutter/test/task021_store_profile_contact_test.dart`; `apps/mobile_flutter/test/visual_reference/task021_store_profile_contact_reference_360x780.png` | لا DB/API/RLS جديد؛ يستخدم `getStore(widget.storeId)` وحقول `AssalStoreSummary` الحالية لقنوات الهاتف والروابط الاجتماعية وخيارات التوصيل ونقاط الاستلام | تبويب «التواصل» يعرض «الهاتف» و«واتساب» و«تلغرام» والموقع الإلكتروني من القيم المصدرية، وكل قناة ActionChip مرتبطة بفتح URI/fallback القائم. يعرض التسليم والاستلام من الحقول المصدرية، وزر «مراسلة التاجر» يمر عبر `createConversation` الحالي بعد جلسة دخول؛ لا أفعال حفظ/إلغاء شكلية | loading/نجاح عبر شاشة المتجر، empty منفصل لقنوات التواصل والتسليم، error مع «إعادة المحاولة» المصدرية، تعريب labels، وقنوات مرتبة بـWrap لتفادي overflow على الهاتف | `flutter test --no-pub test/task021_store_profile_contact_test.dart --reporter expanded`: 4 اختبارات وظيفية نجحت؛ golden فشل فقط بنسبة 100.00% (`280800px`). regressions TASK 019 وTASK 020 الوظيفية نجحت؛ `flutter analyze --no-pub`: No issues found | PNG الأصلي 1440×3120 وصُغّر deterministic إلى `360×780`. فشلت المقارنة بنسبة 100.00% (`280800px`) بسبب اختلاف قالب PNG/renderer/بيانات Demo عن actual؛ لم يُعتمد actual كbaseline | لا تغيير DB/API/RLS/الصلاحيات ولا GAP جديد؛ يستمر حجب القبول البصري ضمن فجوة golden القائمة | جزئي — وظيفي ناجح، بصري محجوب بتعارض الأصل/renderer |
| 022 | `screens/022_store_followers.png` + `explanations/022_store_followers.md` | قائمة متابعي المتجر — عميل — هاتف حقيقي مع Bottom Sheet — مرجع 12.6 | `apps/mobile_flutter/lib/features/customer/customer_catalog.dart`; `apps/mobile_flutter/test/task022_store_followers_test.dart`; `apps/mobile_flutter/test/visual_reference/task022_store_followers_reference_360x640.png` | لا DB/API/RLS جديد؛ يستخدم `listStoreFollowers(widget.storeId)` وعقود `AssalStoreFollowersPage` و`AssalStoreFollowerSummary` الحالية، مع حقول الاسم والصورة وتاريخ المتابعة والإجمالي | استُبدلت نافذة AlertDialog القديمة بـBottom Sheet كريمي ذي حواف ناعمة، عنوان «متابعو المتجر»، إجمالي مصدرّي، قائمة متابعين، صور/بدائل، تاريخ المتابعة، وإغلاق حقيقي. لا بيانات ثابتة ولا أفعال تعديل للعميل | loading، success، empty برسالة «لا يوجد متابعون ظاهرون بعد.» مع retry، error برسالة عربية وإعادة محاولة، إجمالي مختصر مثل 2.3K، وتاريخ عربي. تبقى نافذة العرض قابلة للتمرير ومربوطة بمصدر المستودع | `flutter test --no-pub test/task022_store_followers_test.dart --reporter expanded`: 4 اختبارات وظيفية نجحت؛ golden فشل فقط بنسبة 100.00% (`230400px`). regressions TASK 019 وTASK 020 الوظيفية نجحت؛ `flutter analyze --no-pub`: No issues found | PNG الأصلي 1440×2560 وصُغّر deterministic إلى `360×640`. فشلت المقارنة بنسبة 100.00% (`230400px`) بسبب اختلاف قالب PNG/renderer/بيانات Demo عن actual؛ لم يُعتمد actual كbaseline | لا تغيير DB/API/RLS/الصلاحيات ولا GAP جديد؛ يستمر حجب القبول البصري ضمن فجوة golden القائمة | جزئي — وظيفي ناجح، بصري محجوب بتعارض الأصل/renderer |
| 023 | `screens/023_contact_request.png` + `explanations/023_contact_request.md` | نافذة طلب التواصل — عميل — هاتف حقيقي مع Bottom Sheet — مرجع 13 | `apps/mobile_flutter/lib/features/customer/customer_catalog.dart`; `apps/mobile_flutter/test/task023_contact_request_test.dart`; `apps/mobile_flutter/test/visual_reference/task023_contact_request_reference_360x640.png` | لا DB/API/RLS جديد؛ يستخدم `RequestSheet` و`createRequest(requesterId, AssalRequestDraft)` والعقود الحالية؛ يرسل productId/storeId والكمية والهاتف وخيار التسليم وملاحظاته وقناة التواصل وملاحظة السعر | أُعيد تنظيم النافذة بعنوان «اسأل عن التوفر» مع ملخص المنتج والمتجر والصورة 1:1 المصغرة، حقول الرسالة والهاتف والكمية والسعر/ملاحظته والتوصيل، قوائم التسليم والاستلام من بيانات المتجر، وقنوات «داخل عسلكم»/الهاتف/واتساب/تلغرام/الموقع حسب القيم المصدرية. «إرسال الطلب» و«إلغاء» حقيقيان، وبعد النجاح تُغلق النافذة وتظهر رسالة تأكيد عبر ScaffoldMessenger | تحقق الرسالة قبل الإرسال، جلسة مطلوبة، تعطيل الإرسال أثناء الحفظ، نجاح وخطأ createRequest دون فقدان النافذة، حفظ القناة وملاحظة السعر وملاحظة التوصيل في draft، وقابلية تمرير مع keyboard inset | `flutter test --no-pub test/task023_contact_request_test.dart --reporter expanded`: 5 اختبارات وظيفية نجحت؛ golden فشل فقط بنسبة 100.00% (`230400px`). regressions TASK 015 وTASK 016 الوظيفية نجحت؛ `flutter analyze --no-pub`: No issues found | PNG الأصلي 1440×2560 وصُغّر deterministic إلى `360×640`. فشلت المقارنة بنسبة 100.00% (`230400px`) بسبب اختلاف قالب PNG/renderer/بيانات Demo عن actual؛ لم يُعتمد actual كbaseline | لا تغيير DB/API/RLS/الصلاحيات ولا GAP جديد؛ يستمر حجب القبول البصري ضمن فجوة golden القائمة | جزئي — وظيفي ناجح، بصري محجوب بتعارض الأصل/renderer |
| 024 | `screens/024_customer_requests.png` + `explanations/024_customer_requests.md` | قائمة طلبات العميل — عميل — هاتف حقيقي — مرجع 14.1 | `apps/mobile_flutter/lib/features/customer/customer_account.dart`; `apps/mobile_flutter/test/task024_customer_requests_test.dart`; `apps/mobile_flutter/test/visual_reference/task024_customer_requests_reference_360x640.png` | لا DB/API/RLS جديد؛ يستخدم `getSession()` ثم `listRequests(session.user!.id)` و`AssalRequestSummary`/`RequestStatus` الحاليين، مع `listRequestMessages` عند فتح التفاصيل | أُعيد بناء RequestsScreen Stateful بعنوان «طلباتي»، فلاتر «الكل/جديد/قيد الرد/تم الرد/مغلقة»، بطاقات مصدرية ببيانات المنتج والمتجر والكمية والتاريخ والنص، وزر «التفاصيل» ينتقل إلى `CustomerRequestDetailScreen`. حالة الفراغ تعرض «استكشف المنتجات» بانتقال حقيقي إلى SearchScreen؛ لا بيانات ثابتة في الإنتاج | loading/session gate، permission للجلسة غير المصادق عليها، error مع retry، empty للقائمة والقائمة المفلترة، فلترة حقيقية تشمل closed/cancelled، تمهيد طلب واحد فقط وإعادة جلب صريحة، وتمرير RTL وبطاقة صورة 1:1 placeholder عند غياب image URL في عقد الطلب | `flutter test --no-pub test/task024_customer_requests_test.dart --reporter expanded`: 6 اختبارات وظيفية نجحت؛ golden فشل فقط بنسبة 99.99% (`230388px`). regressions TASK 015 وTASK 016 الوظيفية نجحت؛ `flutter analyze --no-pub`: No issues found | PNG الأصلي 1440×2560 وصُغّر deterministic إلى `360×640`. فشلت المقارنة بنسبة 99.99% (`230388px`) بسبب اختلاف قالب PNG/renderer/بيانات Demo عن actual؛ لم يُعتمد actual كbaseline | لا تغيير DB/API/RLS/الصلاحيات ولا GAP جديد؛ لا يملك عقد `AssalRequestSummary` رابط صورة منتج، لذلك استخدم البطاقة placeholder 1:1 بدل جلب API إضافي | جزئي — وظيفي ناجح، بصري محجوب بتعارض الأصل/renderer |
| 025 | `screens/025_customer_request_detail.png` + `explanations/025_customer_request_detail.md` | تفاصيل طلب العميل — عميل — هاتف حقيقي — مرجع 14.2 | `apps/mobile_flutter/lib/features/customer/customer_request_detail.dart`; `apps/mobile_flutter/lib/features/customer/customer_account.dart`; `apps/mobile_flutter/test/task025_customer_request_detail_test.dart`; `apps/mobile_flutter/test/visual_reference/task025_customer_request_detail_reference_360x780.png` | لا DB/API/RLS جديد؛ تُقرأ رسائل الطلب من `listRequestMessages(request.id)`، وتُستخدم `AssalRequestSummary` المصدرية لبيانات المنتج والمتجر والكمية والهاتف وملاحظات السعر/التسليم والتسليم والتواريخ، و`createConversation(userId, storeId)` لمسار المراسلة القائم | أُضيفت بطاقة تفاصيل العميل وحقولها المصدرية، وسجل الردود مع retry، وأفعال «مراسلة التاجر» و«فتح المتجر» مربوطة بمسارات `createConversation`/`ConversationScreen` و`StoreProfileScreen` القائمة عبر callbacks؛ بقي محرر «إجابة التاجر» محصورًا في `merchantMode` ولا تظهر أفعاله للعميل | تحميل الرسائل، نجاح/فراغ، خطأ مع «إعادة المحاولة»، حقول الحالة/الطلب/المنتج/المتجر/الكمية/الهاتف/الهاند أوف/الملاحظات/التواريخ، جلسة المراسلة، إنشاء المحادثة، وفصل صلاحيات العميل عن التاجر | `flutter test --no-pub test/task025_customer_request_detail_test.dart --reporter expanded`: 4 اختبارات وظيفية نجحت؛ golden فشل فقط بنسبة 100.00% (`280800px`). regressions `task024_customer_requests_test.dart` و`task023_contact_request_test.dart`: الوظائف نجحت وgoldens السابقة بقيت محجوبة كما هو موثق؛ `flutter analyze --no-pub`: No issues found | PNG الأصلي 1440×3120 وصُغّر deterministic إلى `360×780`. فشل golden بنسبة 100.00% (`280800px`) بسبب اختلاف قالب PNG/renderer/بيانات Demo عن actual؛ لم يُعتمد actual كbaseline ولم تُصنع مطابقة بصرية مصطنعة | لا تغيير DB/API/RLS/الصلاحيات ولا GAP جديد. عقد `AssalRequestSummary` لا يملك رابط صورة المنتج، لذلك لم يُضف جلب صورة جديد؛ لا أزرار حفظ/إلغاء الشكلية من المرجع. يستمر حجب القبول البصري ضمن فجوة golden القائمة | جزئي — وظيفي ناجح، بصري محجوب بتعارض الأصل/renderer |
| 026 | `screens/026_messages_list.png` + `explanations/026_messages_list.md` | قائمة المحادثات — عميل / تاجر — هاتف حقيقي — مرجع 15.1 | `apps/mobile_flutter/lib/features/customer/customer_account.dart`; `apps/mobile_flutter/lib/core/assal_widgets.dart`; `apps/mobile_flutter/test/task026_messages_list_test.dart`; `apps/mobile_flutter/test/visual_reference/task026_messages_list_reference_360x640.png` | لا DB/API/RLS جديد؛ تستخدم الشاشة `getSession()` ثم `listConversations(session.user!.id)` و`AssalConversationSummary` الحالية، مع `listMessages()` عند فتح `ConversationScreen` القائمة. البحث محلي على `storeName` و`lastMessage` لأن العقد لا يقدم بحث محادثات مستقلًا | أُعيد بناء `MessagesScreen` كـStateful مع جلسة ومصدر محادثات محفوظين، رأس «الرسائل»، حقل «البحث في المحادثات»، بطاقات تعرض اسم المتجر وآخر رسالة ووقت التحديث وعدّاد غير المقروء، وانتقال حقيقي إلى `ConversationScreen`. الإجراء البديل للفراغ ينتقل إلى `StoresScreen` القائمة، ولا تُضاف تفاصيل إنشاء/إرسال محادثة خارج نطاق TASK 026 | loading، session gate، session unavailable مع retry، success، empty مع «استكشف المتاجر»، filter-empty، error مع retry، بحث محلي، refresh للمصدر، وتفادي overflow في بطاقة المحادثة على الهاتف | `flutter test --no-pub test/task026_messages_list_test.dart --reporter expanded`: 6 اختبارات وظيفية نجحت؛ golden فشل فقط بنسبة 99.99% (`230382px`). regressions TASK 025 وTASK 024: الوظائف نجحت وgoldens السابقة بقيت محجوبة كما هو موثق؛ `flutter analyze --no-pub`: No issues found | PNG الأصلي 1440×2560 وصُغّر deterministic إلى `360×640`. فشل golden بنسبة 99.99% (`230382px`) بسبب اختلاف قالب PNG/renderer/بيانات Demo عن actual؛ لم يُعتمد actual كbaseline ولم تُصنع مطابقة بصرية مصطنعة | لا تغيير DB/API/RLS/الصلاحيات ولا GAP جديد. `AssalConversationSummary` لا يملك رابط صورة/هوية متجر، لذلك استُخدم placeholder المشترك بدل اختراع صورة؛ لم تُنقل محادثة المرجع المفتوحة إلا عبر الانتقال الحقيقي إلى TASK 027 | جزئي — وظيفي ناجح، بصري محجوب بتعارض الأصل/renderer |
| 027 | `screens/027_conversation.png` + `explanations/027_conversation.md` | شاشة المحادثة — عميل / تاجر — هاتف حقيقي — مرجع 15.2 | `apps/mobile_flutter/lib/features/customer/customer_account.dart`; `apps/mobile_flutter/test/task027_conversation_test.dart`; `apps/mobile_flutter/test/visual_reference/task027_conversation_reference_360x780.png` | لا DB/API/RLS جديد؛ تستخدم `getSession()`، و`listMessages(conversation.id)`، و`sendMessage(userId, AssalMessageDraft)` والعقود الحالية للرسائل والجلسة | أُعيد بناء `ConversationScreen` لعرض رأس المحادثة وحالة الاتصال وفق الجلسة، فقاعات الرسائل المصدرية، محرر «اكتب رسالتك»، وإرسال حقيقي يعيد قراءة الرسائل بعد النجاح. يُعطّل المحرر والإرسال لغير المصادق مع مسار تسجيل الدخول القائم؛ لا أفعال حفظ/إلغاء شكلية | loading، session unavailable مع retry، empty برسالة عربية وإجراء بدء المحادثة، error مع retry، بوابة جلسة، نجاح الإرسال، خطأ الإرسال مع إبقاء النص، تعطيل الإرسال المكرر، وتحديث المصدر بعد الإرسال | `flutter test --no-pub test/task027_conversation_test.dart --reporter expanded`: 7 اختبارات وظيفية نجحت؛ golden فشل فقط بنسبة 100.00% (`280800px`). regressions TASK 026 وTASK 025: الوظائف نجحت وgoldens السابقة بقيت محجوبة كما هو موثق؛ `flutter analyze --no-pub`: No issues found | PNG الأصلي 1440×3120 وصُغّر deterministic إلى `360×780`. فشل golden بنسبة 100.00% (`280800px`) بسبب اختلاف قالب PNG/renderer/بيانات Demo عن actual؛ لم يُعتمد actual كbaseline ولم تُصنع مطابقة بصرية مصطنعة | لا تغيير DB/API/RLS/الصلاحيات ولا GAP جديد. `AssalConversationSummary` لا يملك رابط صورة، لذلك بقي placeholder المشترك؛ لم تُضف قناة إرسال أو مرفقات غير موجودة في العقد | جزئي — وظيفي ناجح، بصري محجوب بتعارض الأصل/renderer |
| 028 | `screens/028_new_conversation.png` + `explanations/028_new_conversation.md` | إنشاء محادثة لأول مرة — عميل — هاتف حقيقي مع نافذة داخلية — مرجع 15.3 | `apps/mobile_flutter/lib/features/customer/customer_account.dart`; `apps/mobile_flutter/lib/features/customer/customer_catalog.dart`; `apps/mobile_flutter/test/task028_new_conversation_test.dart`; `apps/mobile_flutter/test/visual_reference/task028_new_conversation_reference_360x780.png` | لا DB/API/RLS جديد؛ تستخدم النافذة `AssalStoreSummary` المصدرية، ثم `createConversation(userId, storeId)` و`sendMessage(userId, AssalMessageDraft)` وعقود الجلسة الحالية | أُضيفت `NewConversationSheet` للعميل بعنوان «مراسلة التاجر»، اسم المتجر، حقل «أول رسالة»، تحقق من عدم الفراغ، «بدء المحادثة» و«إلغاء» حقيقيان. رُبط زر مراسلة التاجر في تبويب المتجر بالنافذة، ثم إنشاء المحادثة وإرسال الرسالة الأولى والانتقال إلى `ConversationScreen`; خطأ الإنشاء يبقى في الشاشة دون انتقال | تحقق الرسالة الأولى، session gate عبر `requireUserSession`، تعطيل مسار المتجر أثناء الإنشاء، نجاح/خطأ `createConversation`، إرسال الرسالة الأولى، انتقال حقيقي للمحادثة، وإلغاء حقيقي | `flutter test --no-pub test/task028_new_conversation_test.dart --reporter expanded`: 4 اختبارات وظيفية نجحت؛ golden فشل فقط بنسبة 100.00% (`280800px`); `flutter analyze --no-pub`: No issues found | PNG الأصلي 1440×3120 وصُغّر deterministic إلى `360×780`. فشل golden بنسبة 100.00% (`280800px`) بسبب اختلاف قالب PNG/renderer/بيانات Demo عن actual؛ لم يُعتمد actual كbaseline ولم تُصنع مطابقة بصرية مصطنعة | لا تغيير DB/API/RLS/الصلاحيات ولا GAP جديد. لم يُنقل زر «حفظ» الشكلي من المرجع؛ لا بيانات متجر أو أفعال مراسلة جديدة خارج العقود القائمة | جزئي — وظيفي ناجح، بصري محجوب بتعارض الأصل/renderer |
| 029 | `screens/029_saved_items.png` + `explanations/029_saved_items.md` | المحفوظات — عميل — هاتف حقيقي — مرجع 16.1 | `apps/mobile_flutter/lib/features/customer/customer_favorites.dart`; `apps/mobile_flutter/lib/core/assal_widgets.dart`; `apps/mobile_flutter/test/task029_saved_items_test.dart`; `apps/mobile_flutter/test/visual_reference/task029_saved_items_reference_360x780.png` | لا DB/API/RLS جديد؛ تستخدم `getSession()` المحفوظة، و`listFavoriteProducts` و`listFollowedStores` و`listFavoriteTaxonomies`، و`toggleFavorite`/`toggleFollow` والعقود الحالية | حُصّنت شاشة «المحفوظات والمتابعات» بمصدر جلسة محفوظ، حالات source موحدة، تبويبات المنتجات والمتاجر والتصنيفات القائمة، بطاقات مصدرية بصور المنتجات 1:1، فتح التفاصيل، أفعال إزالة الحفظ وإزالة المتابعة، إجراء «استكشف المنتجات» الحقيقي، وتحديث المصدر بعد النجاح. عُدلت ProductCard لتعرض «إزالة الحفظ» وحالة القلب عند استخدامها من المحفوظات، وعولج overflow مثبت في الشبكة | loading، session gate وتسجيل الدخول بعد العودة، session unavailable/retry، empty مع إجراء بديل، error/retry لكل مصدر، فتح ProductDetailScreen وStoreProfileScreen، منع الإزالة المكررة، ونجاح/خطأ أفعال الحفظ والمتابعة | `flutter test --no-pub test/task029_saved_items_test.dart --reporter expanded`: 5 اختبارات وظيفية نجحت؛ golden فشل فقط بنسبة 100.00% (`280800px`); `flutter analyze --no-pub`: No issues found | PNG الأصلي 1440×3120 وصُغّر deterministic إلى `360×780`. فشل golden بنسبة 100.00% (`280800px`) بسبب اختلاف قالب PNG/renderer/بيانات Demo عن actual؛ لم يُعتمد actual كbaseline ولم تُصنع مطابقة بصرية مصطنعة | لا تغيير DB/API/RLS/الصلاحيات ولا GAP جديد. أُبقي تبويب التصنيفات القائمة للتوافق مع `initialTab`، ولم تُضف صور أو حقول غير موجودة في العقود | جزئي — وظيفي ناجح، بصري محجوب بتعارض الأصل/renderer |
| 030 | `screens/030_following.png` + `explanations/030_following.md` | المتابعات — عميل — هاتف حقيقي — مرجع 16.2 | `apps/mobile_flutter/lib/features/customer/customer_favorites.dart`; `apps/mobile_flutter/test/task030_following_test.dart`; `apps/mobile_flutter/test/visual_reference/task030_following_reference_360x780.png` | لا DB/API/RLS جديد؛ تستخدم `getSession()` و`listFollowedStores(userId)` و`toggleFollow(userId, storeId)` و`listProducts(query: AssalProductQuery(storeId: ...))` لعدد المنتجات من المصدر | طُوّر تبويب المتابعات داخل `FavoritesScreen` لعرض اسم المتجر وأعداد المتابعين والمنتجات والتقييم من العقود، فتح `StoreProfileScreen`، وإلغاء المتابعة الحقيقي مع إعادة القراءة. أضيف إجراء «اكتشف المتاجر» الحقيقي إلى `StoresScreen` عند الفراغ، مع إبقاء صورة المتجر/الكيان ضمن البطاقة الموحدة | loading، session gate، session unavailable/retry، empty مع «اكتشف المتاجر»، error/retry، حساب عدد المنتجات من المصدر، منع الإزالة المكررة، نجاح/خطأ `toggleFollow`، وفتح المتجر الحقيقي | `flutter test --no-pub test/task030_following_test.dart --reporter expanded`: 6 اختبارات وظيفية نجحت؛ golden فشل فقط بنسبة 100.00% (`280800px`); `flutter analyze --no-pub`: No issues found | PNG الأصلي 1440×3120 وصُغّر deterministic إلى `360×780`. فشل golden بنسبة 100.00% (`280800px`) بسبب اختلاف قالب PNG/renderer/بيانات Demo عن actual؛ لم يُعتمد actual كbaseline ولم تُصنع مطابقة بصرية مصطنعة | لا تغيير DB/API/RLS/الصلاحيات ولا GAP جديد. عدد المنتجات يُقرأ عبر `listProducts` القائم لأن `AssalStoreSummary` لا يملك حقل productCount؛ لا أفعال شكلية جديدة | جزئي — وظيفي ناجح، بصري محجوب بتعارض الأصل/renderer |
| 031 | `screens/031_profile.png` + `explanations/031_profile.md` | الملف الشخصي — عميل — هاتف حقيقي — مرجع 17.1–17.3 | `apps/mobile_flutter/lib/features/customer/customer_account.dart`; `apps/mobile_flutter/test/task031_profile_test.dart`; `apps/mobile_flutter/test/visual_reference/task031_profile_reference_360x640.png` | لا DB/API/RLS جديد؛ تستخدم `AssalSession`/`AssalUserProfile` المصدرية و`listFollowedStores` و`listFavoriteProducts` و`listRequests` لإحصائيات النشاط، مع المسارات القائمة للمحفوظات والمتابعات والطلبات والرسائل والإشعارات والدعم | حُصّنت `ProfileScreen` بمصدر جلسة محفوظ وحالة unavailable مع retry وعودة بعد تسجيل الدخول/الخروج. عُرضت حقول الملف المصدرية عبر `AssalProfileHeaderCard`، ونُظمت بطاقة «نشاطك» بأفعال المحفوظات والمتابعات وطلباتي والرسائل والإشعارات، ثم «الحساب والمساعدة» بالإعدادات والمساعدة والدعم الفني، مع انتقالات حقيقية | loading، session unavailable/retry، guest/auth session، حقول الاسم/الدور/البريد/الهاتف/الموقع/النبذة/المتابعين، إحصائيات المصدر، فتح المسارات، وبقاء أفعال الصور والتعديل والخروج والحذف القائمة ضمن صلاحيات العميل | `flutter test --no-pub test/task031_profile_test.dart --reporter expanded`: 4 اختبارات وظيفية نجحت؛ golden فشل فقط بنسبة 100.00% (`230395px`); `flutter analyze --no-pub`: No issues found | PNG الأصلي 1440×2560 وصُغّر deterministic إلى `360×640`. فشل golden بنسبة 100.00% (`230395px`) بسبب اختلاف قالب PNG/renderer/بيانات Demo عن actual؛ لم يُعتمد actual كbaseline ولم تُصنع مطابقة بصرية مصطنعة | لا تغيير DB/API/RLS/الصلاحيات ولا GAP جديد. لم تُضف أفعال حذف أو تعديل جديدة، وتم استخدام SupportCenterScreen القائم لكل من المساعدة والدعم الفني | جزئي — وظيفي ناجح، بصري محجوب بتعارض الأصل/renderer |
| 032 | `screens/032_profile_editor.png` + `explanations/032_profile_editor.md` | محرر الملف الشخصي — عميل — هاتف حقيقي — مرجع 17.4 | `apps/mobile_flutter/lib/features/customer/customer_account.dart`; `apps/mobile_flutter/test/task032_profile_editor_test.dart`; `apps/mobile_flutter/test/visual_reference/task032_profile_editor_reference_360x780.png` | لا DB/API/RLS جديد؛ يستخدم `AssalUserProfile` و`AssalUserProfilePatch` و`updateUserProfile(userId, patch)` و`uploadMerchantImage` الحالية، دون إضافة حقول أو صلاحيات | أُضيف `Form` موحد إلى `ProfileEditorScreen`، وحقول الاسم العربي/النبذة/الهاتف/الموقع مع قيم المصدر، hints عربية، validator للاسم، اختيار الموقع القائم، زرا «إلغاء» و«حفظ التغييرات» حقيقيان. الحفظ ي trim القيم، يعطل التكرار، يرفع الصور المحددة بالعقد ثم يرسل patch للمستخدم، يعرض نجاحًا ويعود للمسار السابق؛ الخطأ يبقي المستخدم في الشاشة | عرض القيم الحالية، التحقق من الاسم الفارغ، تعديل الحقول، اختيار الموقع، الإلغاء، الحفظ، تعطيل الحفظ أثناء الطلب، إعادة استخدام upload/update القائمة، وبقاء صور المنتجات 1:1 خارج نطاق المحرر | `flutter test --no-pub test/task032_profile_editor_test.dart`: 4 اختبارات وظيفية نجحت؛ golden فشل فقط بنسبة 100.00% (`280800px`). `flutter analyze --no-pub`: No issues found | PNG الأصلي 1440×3120 وصُغّر حتميًا إلى `360×780`. فشل golden بسبب اختلاف قالب PNG/renderer/بيانات Demo عن actual؛ لم يُعتمد actual كbaseline ولم تُصنع مطابقة بصرية مصطنعة | لا تغيير DB/API/RLS/الصلاحيات ولا GAP جديد. مكون الصور الحالي يعرض زرًا صغيرًا داخل الصورة مع semantics حقيقية؛ لم تُضف حقول اسم إنجليزي غير موجودة في `AssalUserProfile` | جزئي — وظيفي ناجح، بصري محجوب بتعارض الأصل/renderer |
| 033 | `screens/033_notifications.png` + `explanations/033_notifications.md` | الإشعارات — عميل / تاجر — هاتف حقيقي — مرجع 18 | `apps/mobile_flutter/lib/features/customer/customer_account.dart`; `apps/mobile_flutter/test/task033_notifications_test.dart`; `apps/mobile_flutter/test/visual_reference/task033_notifications_reference_360x780.png` | لا DB/API/RLS جديد؛ يستخدم `getSession()` و`listNotifications(userId)` و`markNotificationRead(userId, notificationId)` الحالية، دون إضافة endpoint لتحديد الكل | أُعيد بناء `NotificationsScreen` كحالة Stateful بمستقبل جلسة ومصدر إشعارات محفوظين، مع عنوان RTL، بطاقات `AssalNotificationCard`، زر «تحديد الكل كمقروء» ينفذ `markNotificationRead` لكل unread بالتتابع ثم يعيد القراءة، وقراءة فردية تعيد المصدر بعد النجاح. أضيفت حالة فراغ مع «استكشف السوق» إلى `HomeScreen` القائم، retry للجلسة والمصدر، وتعطيل التكرار أثناء الحفظ؛ لم تُنقل أفعال حفظ/إلغاء الشكلية من المرجع | loading، session unavailable مع retry، guest gate، data/empty/error/retry، إشعارات جديدة ومقروءة، قراءة فردية، تحديد الكل كمقروء، إعادة تحميل المصدر بعد النجاح، ورسالة خطأ دون ادعاء نجاح | `flutter test --no-pub test/task033_notifications_test.dart`: 5 اختبارات وظيفية نجحت؛ golden فشل فقط بنسبة 100.00% (`280799px`). `flutter analyze --no-pub`: No issues found | PNG الأصلي 1440×3120 وصُغّر حتميًا إلى `360×780`. فشل golden بسبب اختلاف قالب PNG/renderer/بيانات Demo عن actual؛ لم يُعتمد actual كbaseline ولم تُصنع مطابقة بصرية مصطنعة | لا تغيير DB/API/RLS/الصلاحيات ولا GAP جديد. تحديد الكل يستخدم العقد الفردي الموجود بدل اختراع عملية bulk، وبقيت الصور/الروابط المصدرية فقط | جزئي — وظيفي ناجح، بصري محجوب بتعارض الأصل/renderer |
| 034 | `screens/034_settings.png` + `explanations/034_settings.md` | الإعدادات — عميل — هاتف حقيقي — مرجع 19 | `apps/mobile_flutter/lib/features/customer/customer_account.dart`; `apps/mobile_flutter/lib/features/customer/customer_discovery.dart`; `apps/mobile_flutter/test/task034_settings_test.dart`; `apps/mobile_flutter/test/visual_reference/task034_settings_reference_360x780.png` | لا DB/API/RLS جديد؛ تستخدم `getSession()` و`signOut()` و`deleteAccount()` و`listNotifications()` و`AssalUserProfile` الحالية، بينما تفضيلات الخصوصية/تفصيل الإشعارات تبقى محلية للجلسة لغياب عقد حفظ مستقل | أُعيد بناء `SettingsScreen` بمستقبل جلسة محفوظ، بطاقة الحساب والبريد والهاتف، أقسام الخصوصية وإظهار الاسم/الصورة وإعدادات المتابعين، الإشعارات والطلبات/الرسائل/التفاعل، الأمان، اللغة والمظهر وعن عسلكم. رُبط فتح محرر الملف والإشعارات وتسجيل الخروج وحذف الحساب بمسارات/طلبات حقيقية، وأضيف حفظ صريح للتفضيلات المحلية برسالة نطاق صادقة، مع تمرير repository من ProfileScreen وHomeScreen | loading، guest gate، session unavailable/retry، الحقول المصدرية، تبديل التفضيلات، حفظ الجلسة، فتح الإشعارات، حوار الأمان وعن التطبيق، تسجيل الخروج وحذف الحساب، وتعطيل الأفعال أثناء الطلب | `flutter test --no-pub test/task034_settings_test.dart`: 5 اختبارات وظيفية نجحت؛ golden فشل فقط بنسبة 100.00% (`280800px`). `flutter analyze --no-pub`: No issues found | PNG الأصلي 1440×3120 وصُغّر حتميًا إلى `360×780`. فشل golden بسبب اختلاف قالب PNG/renderer/بيانات Demo عن actual؛ لم يُعتمد actual كbaseline ولم تُصنع مطابقة بصرية مصطنعة | لا تغيير DB/API/RLS/الصلاحيات ولا GAP جديد. لم يُدّع حفظ دائم لتفضيلات لا يملك لها `AssalRepository` عقدًا؛ بقيت اللغة/المظهر للقراءة ضمن حدود التطبيق الحالي | جزئي — وظيفي ناجح، بصري محجوب بتعارض الأصل/renderer |
| 035 | `screens/035_support.png` + `explanations/035_support.md` | الدعم الفني — عميل — هاتف حقيقي — مرجع 20.1 | `apps/mobile_flutter/lib/features/customer/customer_support.dart`; `apps/mobile_flutter/test/task035_support_test.dart`; `apps/mobile_flutter/test/visual_reference/task035_support_reference_360x780.png` | لا DB/API/RLS جديد؛ يستخدم `getSession()` الحالية، ولا يوجد عقد مستقل لإنشاء تذكرة دعم أو رفع مرفق، وهو GAP-005 مؤكد | أُعيد بناء `SupportCenterScreen` كحالة Stateful بمصدر جلسة محفوظ، حاجز ضيف وretry، أسئلة مساعدة قابلة للتوسيع، نموذج موضوع/تفاصيل مع تحقق، مسار إرفاق يعرض حدود العقد بدل فتح رفع شكلي، وإرسال يعرض رسالة صادقة بعد التحقق لأن `AssalRepository` لا يملك خدمة تذاكر. بقي التواصل بالبريد الرسمي والمساعدة العامة، وطلب التصميم القائم ضمن نطاقه | loading، session unavailable/retry، guest gate، نموذج فارغ/متحقق، حد المرفقات، حد إرسال التذكرة، طلبات الدعم السابقة غير المتاحة، التواصل بالبريد، والمساعدة العامة؛ لا نجاح مزيف أو انتقال غير موصول | `flutter test --no-pub test/task035_support_test.dart`: 5 اختبارات وظيفية نجحت؛ golden فشل فقط بنسبة 100.00% (`280800px`). `flutter analyze --no-pub`: No issues found | PNG الأصلي 1440×3120 وصُغّر حتميًا إلى `360×780`. فشل golden بسبب اختلاف قالب PNG/renderer/بيانات Demo عن actual؛ لم يُعتمد actual كbaseline ولم تُصنع مطابقة بصرية مصطنعة | GAP-005 قائم: لا عقد تذاكر دعم/رفع مرفقات. لا تغيير DB/API/RLS/الصلاحيات؛ يلزم قرار وعقد مستقل قبل تفعيل الإرسال أو سجل الطلبات | جزئي — وظيفي ناجح، بصري محجوب بتعارض الأصل/renderer |
| 036 | `screens/036_help.png` + `explanations/036_help.md` | المساعدة — عميل — هاتف حقيقي — مرجع 20.2 | `apps/mobile_flutter/lib/features/customer/customer_support.dart`; `apps/mobile_flutter/lib/features/customer/customer_account.dart`; `apps/mobile_flutter/test/task036_help_test.dart`; `apps/mobile_flutter/test/visual_reference/task036_help_reference_360x780.png` | لا DB/API/RLS جديد؛ يعتمد `getSession()` الحالية، ومحتوى الأسئلة المحلي الثابت داخل الواجهة، ومسار `SupportCenterScreen` القائم للتواصل بسبب GAP-005 | أُضيف `HelpScreen` مستقل بعنوان «المساعدة»، بحث محلي في الأسئلة، ست فئات مطابقة للعقد: الحساب والدخول، المتاجر والمنتجات، الطلبات والمراسلات، الصور والرفع، الخصوصية، والتواصل مع الدعم. كل سؤال قابل للتوسيع بإرشاد عربي، والبحث يوضح الفراغ، وزر التواصل يفتح مركز الدعم الحقيقي. رُبطت أزرار المساعدة من AuthScreen وProfileScreen بالمسار الجديد | loading، session unavailable/retry، محتوى الأسئلة، البحث المطابق/الفراغ، التوسيع، الانتقال إلى الدعم، وقراءة الضيف دون ادعاء مصدر بيانات أو تذكرة جديدة | `flutter test --no-pub test/task036_help_test.dart`: 5 اختبارات وظيفية نجحت؛ golden فشل فقط بنسبة 100.00% (`280800px`). `flutter analyze --no-pub`: No issues found | PNG الأصلي 1440×3120 وصُغّر حتميًا إلى `360×780`. فشل golden بسبب اختلاف قالب PNG/renderer/بيانات Demo عن actual؛ لم يُعتمد actual كbaseline ولم تُصنع مطابقة بصرية مصطنعة | GAP-005 مستمر لخدمة التذاكر؛ محتوى المساعدة محلي ومعلوماتي لأن عقد محتوى المساعدة غير موجود، ولا تغيير DB/API/RLS/الصلاحيات | جزئي — وظيفي ناجح، بصري محجوب بتعارض الأصل/renderer |
| 037 | `screens/037_design_request.png` + `explanations/037_design_request.md` | طلب تصميم إضافي — عميل — هاتف حقيقي — مرجع 20.3 | `apps/mobile_flutter/lib/features/customer/customer_support.dart`; `apps/mobile_flutter/test/task037_design_request_test.dart`; `apps/mobile_flutter/test/visual_reference/task037_design_request_reference_360x780.png` | لا DB/API/RLS جديد؛ يستخدم `createDesignRequest(userId, storeId, AssalDesignRequestDraft)` الحالي بما فيه `title` و`description` و`brandName` و`brandColors` و`productScope`. لا يوجد عقد رفع مرجع/ملف أو قراءة الطلبات السابقة | حُصّن `DesignRequestScreen` بـ`Form` والتحقق من العنوان والوصف، وأضيف نطاق التصميم إلى `productScope` المدعوم، مع حقول العلامة والألوان، وحد رفع المرجع ورسالة سجل الطلبات السابقة الصادقة عند غياب عقد مستقل. الإرسال يمر عبر الجلسة و`createDesignRequest` الحقيقي، يعطل التكرار، يعرض نتيجة المصدر، ثم يعود عند النجاح | عرض الحقول، التحقق، إرسال draft مع نطاق التصميم، جلسة unavailable، حد رفع المرجع، وحد سجل الطلبات السابقة؛ لا fake success أو رفع غير موصول | `flutter test --no-pub test/task037_design_request_test.dart`: 5 اختبارات وظيفية نجحت؛ golden فشل فقط بنسبة 100.00% (`280800px`). `flutter analyze --no-pub`: No issues found | PNG الأصلي 1440×3120 وصُغّر حتميًا إلى `360×780`. فشل golden بسبب اختلاف قالب PNG/renderer/بيانات Demo عن actual؛ لم يُعتمد actual كbaseline ولم تُصنع مطابقة بصرية مصطنعة | GAP-005 يغطي غياب خدمة الرفع/السجل؛ لا تغيير DB/API/RLS/الصلاحيات، و`productScope` استُخدم كما هو معرف في العقد | جزئي — وظيفي ناجح، بصري محجوب بتعارض الأصل/renderer |
| 038 | `screens/038_merchant_store_opening.png` + `explanations/038_merchant_store_opening.md` | فتح أو إنشاء مساحة متجر — تاجر — هاتف حقيقي — مرجع 21.1 | `apps/mobile_flutter/lib/features/customer/customer_account.dart`; `apps/mobile_flutter/test/task038_merchant_store_opening_test.dart`; `apps/mobile_flutter/test/visual_reference/task038_merchant_store_opening_reference_360x780.png` | لا DB/API/RLS جديد؛ يستخدم `getSession()` و`loadMerchantApplicationDraft()` و`saveMerchantApplicationDraft()` و`openMerchantWorkspace()` و`uploadMerchantImage()` و`AssalMerchantApplicationDraft`/`AssalMerchantWorkspaceDraft` الحالية | حُصّنت شاشة فتح مساحة المتجر بجلسة محفوظة وحالات loading/error/retry/guest، واستعادة draft حقيقية، وحقول اسم المتجر والهاتف والموقع والخبرة والتخصصات والوصف، وزر حفظ مؤقت يعيد قراءة المصدر، ورفع غلاف/شعار عبر العقد الموجود، وفتح workspace عبر الطلب الحقيقي. تُعرض سياسة «معلّق حتى تفعيل الإدارة» دون عبارة متجر مفعّل، وبعد النجاح ينتقل المسار إلى `MerchantDashboard` القائم | loading، session unavailable/retry، guest gate، draft فارغ/مستعاد، تحقق اسم المتجر، حفظ الحقول المدعومة، رفع الصور، منع التكرار، فتح مساحة المتجر وحالة النشر المعلّق | `flutter test --no-pub test/task038_merchant_store_opening_test.dart`: 6 اختبارات وظيفية نجحت؛ golden فشل فقط بنسبة 100.00% (`280800px`). `flutter analyze --no-pub`: No issues found | PNG الأصلي 1440×3120 وصُغّر حتميًا إلى `360×780`. فشل golden بسبب اختلاف قالب PNG/renderer/بيانات Demo عن actual؛ لم يُعتمد actual كbaseline ولم تُصنع مطابقة بصرية مصطنعة | لا تغيير DB/API/RLS/الصلاحيات ولا GAP جديد. الحالة المعلقة مصدرية من `AssalMerchantWorkspaceSummary`/سياسة التطبيق، ولم تُضف صلاحية نشر أو تفعيل إداري داخل العميل | جزئي — وظيفي ناجح، بصري محجوب بتعارض الأصل/renderer |
| 039 | `screens/039_merchant_dashboard.png` + `explanations/039_merchant_dashboard.md` | مساحة التاجر ونظرة عامة — تاجر — هاتف حقيقي — مرجع 21.2–21.3 | `apps/mobile_flutter/lib/features/merchant/merchant_dashboard.dart`; `apps/mobile_flutter/test/task039_merchant_dashboard_test.dart`; `apps/mobile_flutter/test/visual_reference/task039_merchant_dashboard_reference_360x780.png` | لا DB/API/RLS جديد؛ تستخدم `loadMerchantWorkspace()` و`listMerchantProducts()` و`listMerchantRequests()` و`listComments()` و`updateMerchantWorkspace()` و`uploadMerchantImage()` القائمة | دُققت لوحة التاجر الموحدة ذات تبويبات «نظرة عامة»، «الكتالوج»، «الطلبات»، و«إدارة»، مع إجراءات إضافة المنتج وتعديل المتجر والمعاينة الموحدة وخدمة الدعم، وبطاقات المنتجات والطلبات والتعليقات والإحصاءات. أضيف retry صريح لحالات أخطاء مصادر workspace/products/requests/comments/metrics، وتحديث toolbar يعيد كل المصادر. أزيلت عبارة «متجرك مفعّل» غير المرغوبة واستُبدلت بنص يعتمد الصلاحية وحالة النشر | loading، workspace error/retry، empty workspace، المنتجات الفارغة/الخطأ، الطلبات الفارغة/الخطأ، إدارة المسودات والإحصاءات والتعليقات، metrics المصدرية، تحديث toolbar، منع ادعاء التفعيل، وإجراءات التاجر القائمة | `flutter test --no-pub test/task039_merchant_dashboard_test.dart`: 5 اختبارات وظيفية نجحت؛ golden فشل فقط بنسبة 100.00% (`280800px`). `flutter analyze --no-pub`: No issues found | PNG الأصلي 1440×3120 وصُغّر حتميًا إلى `360×780`. فشل golden بسبب اختلاف قالب PNG/renderer/بيانات Demo عن actual؛ لم يُعتمد actual كbaseline ولم تُصنع مطابقة بصرية مصطنعة | لا تغيير DB/API/RLS/الصلاحيات ولا GAP جديد. الإحصاءات من حقول read model والمنتج المصدرية فقط، والتبويبات لا تضيف صلاحيات نشر أو توثيق | جزئي — وظيفي ناجح، بصري محجوب بتعارض الأصل/renderer |
| 040 | `screens/040_merchant_catalog.png` + `explanations/040_merchant_catalog.md` | كتالوج التاجر — تاجر — هاتف حقيقي — مرجع 21.4 | `apps/mobile_flutter/lib/features/merchant/merchant_dashboard.dart`; `apps/mobile_flutter/test/task040_merchant_catalog_test.dart`; `apps/mobile_flutter/test/visual_reference/task040_merchant_catalog_reference_360x780.png` | لا DB/API/RLS جديد؛ يستخدم `listMerchantProducts(userId)` القائمة، مع بحث/تصفية/ترتيب محلي فوق البيانات المحملة، و`create/update/delete` ومسارات المعاينة الحالية | طُور تبويب كتالوج التاجر بإضافة بحث المنتجات، مرشح الحالة (كل الحالات/مسودة/معلق/منشور/متوقف مؤقتًا/مرفوض)، وترتيب محلي بالترتيب الأساسي أو المشاهدة أو الإعجاب أو الاسم. بقيت صور البطاقات 1:1، وأفعال الإضافة والتعديل والحذف والمعاينة موصولة، وأضيفت حالة عدم وجود نتائج وحالة retry لمصدر المنتجات دون توسيع query أو API | loading، مصدر منتجات error/retry، empty catalog، no-match، بحث محلي، فلاتر الحالة، ترتيب metrics محلي، عرض الصور، وإجراءات المنتج القائمة | `flutter test --no-pub test/task040_merchant_catalog_test.dart`: 5 اختبارات وظيفية نجحت؛ golden فشل فقط بنسبة 100.00% (`280800px`). `flutter analyze --no-pub`: No issues found | PNG الأصلي 1440×3120 وصُغّر حتميًا إلى `360×780`. فشل golden بسبب اختلاف قالب PNG/renderer/بيانات Demo عن actual؛ لم يُعتمد actual كbaseline ولم تُصنع مطابقة بصرية مصطنعة | لا تغيير DB/API/RLS/الصلاحيات ولا GAP جديد. لم يُضف حقل أو filter إلى `AssalProductQuery`؛ التصفية والترتيب محليان على القائمة المصدرية الحالية | جزئي — وظيفي ناجح، بصري محجوب بتعارض الأصل/renderer |
| 041 | `screens/041_merchant_requests.png` + `explanations/041_merchant_requests.md` | طلبات التاجر وتفاصيلها والرد — تاجر — هاتف حقيقي — مرجع 21.5–21.6 | `apps/mobile_flutter/lib/features/merchant/merchant_dashboard.dart`; `apps/mobile_flutter/lib/features/customer/customer_request_detail.dart`; `apps/mobile_flutter/test/task041_merchant_requests_test.dart`; `apps/mobile_flutter/test/visual_reference/task041_merchant_requests_reference_360x780.png` | لا DB/API/RLS جديد؛ يستخدم `listMerchantRequests()` و`listRequestMessages()` و`replyToRequest()` و`AssalRequestReplyDraft` و`RequestResponseCode` الحالية | دُققت قائمة طلبات التاجر وأضافت مرشحًا محليًا لحالات الطلب المصدرية، مع بطاقة تفاصيل موحدة تعرض العميل والكمية وملاحظات العميل وخيار التسليم، وسجل الرسائل، وقائمة حالة الرد «متوفر/غير متوفر/يتطلب التواصل»، ومحرر رد يمر عبر `replyToRequest` الحقيقي ثم يعيد تحميل سجل الطلب. أضيف retry لمصدر الطلبات ورسالة فراغ/عدم تطابق صادقة، مع تعطيل الإرسال أثناء الحفظ | loading، قائمة الطلبات، مرشح الحالات، تفاصيل العميل والكمية والتسليم، سجل الرسائل، الرد مع response code، error/retry، empty/filtered، session gate ومنع التكرار | `flutter test --no-pub test/task041_merchant_requests_test.dart`: 5 اختبارات وظيفية نجحت؛ golden فشل فقط بنسبة 100.00% (`280800px`). `flutter analyze --no-pub`: No issues found | PNG الأصلي 1440×3120 وصُغّر حتميًا إلى `360×780`. فشل golden بسبب اختلاف قالب PNG/renderer/بيانات Demo عن actual؛ لم يُعتمد actual كbaseline ولم تُصنع مطابقة بصرية مصطنعة | لا تغيير DB/API/RLS/الصلاحيات ولا GAP جديد. استخدمت شاشة تفاصيل الطلب وعقد الرد القائمين، ولم تُضف حالات أو قنوات تسليم غير معرّفة | جزئي — وظيفي ناجح، بصري محجوب بتعارض الأصل/renderer |
| 042 | `screens/042_merchant_management.png` + `explanations/042_merchant_management.md` | مسودات ومراجعة وإحصاءات وتعليقات — تاجر — هاتف حقيقي — مرجع 21.7 | `apps/mobile_flutter/lib/features/merchant/merchant_dashboard.dart`; `apps/mobile_flutter/test/task042_merchant_management_test.dart`; `apps/mobile_flutter/test/visual_reference/task042_merchant_management_reference_360x780.png` | لا DB/API/RLS جديد؛ يعتمد `listMerchantProducts()` و`listComments()` و`loadMerchantWorkspace()` القائمة، مع استخدام حقول `ProductStatus` وmetrics في read model | وُسّعت إدارة التاجر إلى تبويبات المسودات والمراجعة، الإحصاءات، التعليقات، حالة المنتج، المشاهدات، الإعجابات، والتقييمات. تعرض المقاييس من المنتجات المصدرية وحالة المنتج بعلامة عربية، وتعيد استخدام retry القائم لمصدر المنتجات والتعليقات، دون أرقام تقديرية أو ادعاء تفعيل | loading، مسودات فارغة/معلقة، مقاييس المنتج، حالة المنتج، المشاهدات، الإعجابات، التقييمات والمراجعات، التعليقات، error/retry، empty، وتحديث المصدر القائم | `flutter test --no-pub test/task042_merchant_management_test.dart`: 5 اختبارات وظيفية نجحت؛ golden فشل فقط بنسبة 100.00% (`280800px`). `flutter analyze --no-pub`: No issues found | PNG الأصلي 1440×3120 وصُغّر حتميًا إلى `360×780`. فشل golden بسبب اختلاف قالب PNG/renderer/بيانات Demo عن actual؛ لم يُعتمد actual كbaseline ولم تُصنع مطابقة بصرية مصطنعة | لا تغيير DB/API/RLS/الصلاحيات ولا GAP جديد. التبويبات والمقاييس تعتمد العقود والحقول الموجودة، والتعليقات تقرأ من `listComments` الحالي | جزئي — وظيفي ناجح، بصري محجوب بتعارض الأصل/renderer |
| 043 | `screens/043_store_editor.png` + `explanations/043_store_editor.md` | محرر المتجر — تاجر — هاتف حقيقي — مرجع 22 | `apps/mobile_flutter/lib/features/merchant/merchant_dashboard.dart`; `apps/mobile_flutter/test/task043_store_editor_test.dart`; `apps/mobile_flutter/test/visual_reference/task043_store_editor_reference_360x780.png` | لا DB/API/RLS جديد؛ يستخدم `listRegions()` و`updateMerchantWorkspace()` و`saveMerchantStoreChannels()` و`uploadMerchantImage()` و`uploadStoreGalleryImage()` الحالية | حُصّن محرر المتجر ببوابة `workspace.canEdit`، وتعطيل الحقول والأفعال أثناء الحفظ/الرفع، وإظهار بيانات الخبرة والتخصصات من read model، وحالات مصدر المناطق مع retry، والتحقق من روابط القنوات، وحفظ workspace والقنوات ثم إعادة النتيجة `true` للوحة التاجر لإعادة القراءة. أضيفت إضافة صورة معرض موصولة بالعقد مع توضيح صريح لغياب حذف الصورة | الحقول الأساسية، الوصف، الهاتف، المحافظة/المديرية، الخبرة والتخصصات المصدرية، واتساب/تيليجرام/الموقع، طرق التسليم، نقاط الاستلام، الغلاف والشعار، إضافة صور المعرض، error/retry، permission/read-only، منع التكرار، save/cancel flow | `flutter test --no-pub test/task043_store_editor_test.dart`: 5 اختبارات وظيفية نجحت؛ golden فشل بنسبة 99.99% (`280780px`). `flutter analyze --no-pub`: No issues found | PNG الأصلي 1440×3120 وصُغّر حتميًا إلى `360×780`. فشل golden بسبب اختلاف قالب PNG/renderer/بيانات Demo عن actual؛ لم يُعتمد actual كbaseline ولم تُصنع مطابقة بصرية مصطنعة | GAP-021: العقد يطلب حذف صور المعرض، لكن `AssalRepository` لا يملك عملية حذف؛ لذلك لم يُضف زر شكلي أو حذف محلي مضلل. لا تغيير DB/API/RLS/الصلاحيات | جزئي — وظيفي ناجح، بصري محجوب بتعارض الأصل/renderer، وحذف المعرض محجوب بعقد مستقل |
| 044 | `screens/044_product_editor_basic.png` + `explanations/044_product_editor_basic.md` | محرر المنتج: البيانات الأساسية — تاجر — هاتف حقيقي — مرجع 23 | `apps/mobile_flutter/lib/features/merchant/merchant_product_editor.dart`; `apps/mobile_flutter/test/task044_product_editor_basic_test.dart`; `apps/mobile_flutter/test/visual_reference/task044_product_editor_basic_reference_360x780.png` | لا DB/API/RLS جديد؛ يستخدم `AssalProductDraft` و`listTaxonomy()` و`listRegions()` و`createMerchantProduct()`/`updateMerchantProduct()` و`uploadProductImage()` الحالية. لم تُضف حقول أو عقود جديدة. | دُققت شاشة محرر المنتج الأساسية RTL مع حقول الاسم العربي، الاسم الإنجليزي الاختياري، الوصف، نوع المنتج والتصنيف المصدرّي، مع بقاء الصور 1:1 ضمن المسار الحالي. أضيف زر إلغاء يعيد `false` صراحة، وأضيفت حالات تحميل/فراغ/خطأ وإعادة محاولة لمصدر التصنيفات؛ retry يحدّث `Future` داخل callback متزامن، والحفظ القائم يبقى معطلًا أثناء الطلب ويستخدم العقد الحقيقي. | عرض الشاشة والحقول، تحقق الاسم العربي، حالة فراغ التصنيفات، زر إعادة المحاولة، إلغاء دون حفظ، وبقاء التعديل محصورًا في العقد القائم؛ لا ادعاء نجاح إنشاء/تحديث في اختبار الضيف ولا أفعال شكلية | `flutter test test/task044_product_editor_basic_test.dart` الوظيفي: 3 اختبارات وظيفية مستقلة نجحت؛ `flutter analyze lib/features/merchant/merchant_product_editor.dart`: No issues found. regression `merchant_product_editor_dropdown_test.dart` و`merchant_product_editable_suggestions_test.dart` نجحا. golden TASK 044 شُغّل دون `--update-goldens` وفشل بنسبة 100.00% (`280800px`) | PNG الأصلي 1440×3120 (نسبة 6:13) وقُرئت 3 بلاطات رأسية بالترتيب؛ المرجع المصغّر 360×780 أُنشئ حتميًا عبر Pillow. فشل golden بسبب تعارض قالب المرجع/renderer/بيانات العرض، ولم يُعتمد actual كbaseline أو نجاح بصري | لا تغيير DB/API/RLS/الصلاحيات ولا GAP جديد. لا توجد فجوة عقدية مثبتة في نطاق البيانات الأساسية؛ حدود حذف الصور محفوظة من GAP-021 ولا عولجت هنا | جزئي — وظيفي ناجح، بصري محجوب بتعارض الأصل/renderer |
| 045 | `screens/045_product_editor_quality.png` + `explanations/045_product_editor_quality.md` | محرر المنتج: المصدر والجودة — تاجر — هاتف حقيقي — مرجع 23 | `apps/mobile_flutter/lib/features/merchant/merchant_product_editor.dart`; `apps/mobile_flutter/test/task045_product_editor_quality_test.dart`; `apps/mobile_flutter/test/visual_reference/task045_product_editor_quality_reference_360x780.png` | لا DB/API/RLS جديد؛ أُعيد استخدام `listRegions()` و`AssalRegion.parentRegionId` و`AssalProductDraft.metadata` ومساري `createMerchantProduct`/`updateMerchantProduct` القائمين. | حُصنت منطقة المصدر والجودة بحالات تحميل «جارٍ تحميل المناطق...»، الخطأ العربي مع «إعادة المحاولة»، والفراغ مع بديل قابل لإعادة القراءة. أضيفت استعادة اختيار المنطقة من `regionNameAr` في read model عند غياب المعرف، مع إبقاء حقول الهوية/المصدر/المعالجة/التغليف/التواريخ/الصلاحية/المكونات/الشهادات مرتبطة بمفاتيح metadata الموجودة. | تحميل/نجاح/فراغ/خطأ/retry للمناطق، حقول المصدر والجودة، cascading المحافظة/المديرية، والإلغاء دون حفظ. لا أضيفت حقول أو عقود جديدة، ولا ادعاء مزامنة غير موجودة. | `flutter analyze lib/features/merchant/merchant_product_editor.dart test/task045_product_editor_quality_test.dart`: نجح. الاختبارات الوظيفية الثلاثة في `task045_product_editor_quality_test.dart`: نجحت (exit codes 0,0,0). regression `merchant_product_editable_suggestions_test.dart`: نجح. | golden `TASK 045 records the visual contract` فشل بنسبة **100.00% = 280800px**. المرجع 1440×3120 صُغّر حتميًا إلى 360×780، وactual renderer أظهر الحروف العربية كمربعات واختلف القالب عن المرجع؛ لم يُعتمد actual كـbaseline ولم تُسجل مطابقة بصرية. | لا تغيير DB/API/RLS/الصلاحيات. يستمر حجب القبول البصري ضمن تعارض المرجع/renderer، مع توثيق actual وmaster في `test/failures` أثناء الفحص فقط. | جزئي — وظيفي ناجح، بصري محجوب بتعارض الأصل/renderer |
| 046 | `screens/046_product_editor_sales.png` + `explanations/046_product_editor_sales.md` | محرر المنتج: السعر والتوفر — تاجر — هاتف حقيقي — مرجع 23 | `apps/mobile_flutter/lib/features/merchant/merchant_product_editor.dart`; `apps/mobile_flutter/test/task046_product_editor_sales_test.dart`; `apps/mobile_flutter/test/merchant_product_editor_dropdown_test.dart`; `apps/mobile_flutter/test/visual_reference/task046_product_editor_sales_reference_360x780.png` | لا DB/API/RLS جديد؛ أُعيد استخدام `AssalProductDraft.metadata` ومساري `createMerchantProduct()`/`updateMerchantProduct()` الحاليين، مع نفس حقول `price` و`currency_code` و`availability` و`delivery_options` و`pickup_locations`. | نُقل السعر والعملة إلى تبويب «البيع والتوصيل» المنفصل، وبقيت حالة التوفر وخيارات التوصيل ونقاط الاستلام ضمن المسار نفسه، مع الحفاظ على التحقق من السعر وتعطيل الإجراء أثناء الحفظ. لم تُضف حقول أو مسارات حفظ جديدة، وبقيت صور المنتجات 1:1 في محرر المنتج الموحد. | عرض السعر والعملة وحالة التوفر وخيارات التوصيل ونقاط الاستلام، منع السعر غير الصالح، أفعال الحفظ والإلغاء الحقيقية، وبقاء التعديل متوافقًا مع اقتراحات تبويب الجودة | `flutter analyze lib/features/merchant/merchant_product_editor.dart test/task046_product_editor_sales_test.dart test/merchant_product_editor_dropdown_test.dart`: نجح. اختبارات TASK 046 الوظيفية الثلاثة: نجحت (exit codes 0,0,0). regressions `merchant_product_editor_dropdown_test.dart` و`merchant_product_editable_suggestions_test.dart` وTASK 044/TASK 045 القريبان: نجحت وظيفيًا. | golden `TASK 046 records the visual contract` فشل بنسبة **100.00% = 280800px** بعد تصغير حتمي من 1440×3120 إلى 360×780؛ لم يُعتمد actual كـbaseline ولم تُسجل مطابقة بصرية. | لا تغيير DB/API/RLS/الصلاحيات ولا GAP جديد. تعارض المرجع/renderer موثق، واختبار dropdown عُدّل فقط ليتوافق مع تقليل الحقول المرئية بعد الفصل دون تغيير عقد البيانات | جزئي — وظيفي ناجح، بصري محجوب بتعارض الأصل/renderer |
| 047 | `screens/047_product_preview_merchant.png` + `explanations/047_product_preview_merchant.md` | معاينة المنتج كما يراه العميل — تاجر — هاتف حقيقي — مرجع 24 | `apps/mobile_flutter/lib/features/customer/customer_catalog.dart`; `apps/mobile_flutter/lib/features/merchant/merchant_dashboard.dart`; `apps/mobile_flutter/test/task047_product_preview_merchant_test.dart`; `apps/mobile_flutter/test/request_sheet_ux_test.dart`; `apps/mobile_flutter/test/visual_reference/task047_product_preview_merchant_reference_360x780.png` | لا DB/API/RLS/صلاحيات جديدة؛ أُعيد استخدام `getProduct()` و`getStore()` و`toggleFavorite()` و`toggleLike()` و`createRequest()` و`listProducts()` القائمة، مع callback المحرر القائم من لوحة التاجر، دون إضافة طريقة repository | ثُبّتت `ProductDetailScreen` كشاشة معاينة موحدة للعميل والتاجر: صورة المنتج 1:1، السعر المنسق، حالة التوفر، خيارات التوصيل/الاستلام، CTA «اسأل عن التوفر»، التبويبات والتفاعل والمنتجات المشابهة. أضيف `merchantMode` اختياري مع زر «تعديل المنتج» موصول إلى `_openProductEditor(storeId, product)` الحقيقي من `_productTile` في `MerchantDashboard`، بينما يفتح `ProductCard` المعاينة الموحدة ولا يُضاف زر مكرر أو فعل شكلي | loading/error/empty/retry للمنتج والمتجر، session gate، معرض 1:1، السعر/التوفر، CTA وRequestSheet، التبويبات والتفاعل، معاينة العميل، زر تعديل التاجر والـcallback الحقيقي، وبقاء التجربة الموحدة حسب الصلاحية | `flutter analyze lib/features/customer/customer_catalog.dart lib/features/merchant/merchant_dashboard.dart test/task047_product_preview_merchant_test.dart`: نجح. اختبارات TASK 047 الوظيفية: اختبار معاينة العميل واختبار تعديل التاجر نجحا. `product_detail_ux_test.dart` و`customer_journey_test.dart` و`request_sheet_ux_test.dart` نجحت؛ ونجحت 5 اختبارات وظيفية في `task039_merchant_dashboard_test.dart`. | golden `TASK 047 records the visual contract` شُغّل دون `--update-goldens` على actual بأبعاد 360×780 بعد ضبط سطح الاختبار؛ فشل بنسبة **100.00% = 280797px**. المرجع 1440×3120 صُغّر حتميًا إلى 360×780، ولم يُعتمد actual كـbaseline. Golden regression للوحة التاجر بقي فاشلًا تاريخيًا بنسبة **100.00% = 280800px** فقط، دون فشل وظيفي في الاختبارات الخمسة | لا تغيير DB/API/RLS/الصلاحيات ولا GAP جديد. `ProductCard` يفتح المعاينة الموحدة، وCTA يبقى سؤالًا/طلبًا حقيقيًا لا إرسالًا ذاتيًا. عُدّلت توقعات `request_sheet_ux_test.dart` المتقادمة فقط لتطابق النصين المصدرين «إرسال الطلب» و«اسأل عن التوفر» دون تعديل الإنتاج | جزئي — وظيفي ناجح، بصري محجوب بتعارض المرجع/renderer/بيانات العرض |
| 048 | `screens/048_admin_login.png` + `explanations/048_admin_login.md` | دخول الإدارة — إدارة — سطح مكتب واقعي — مرجع 23.1 | `apps/admin_web/client/src/components/AdminAuthGate.tsx`; `apps/admin_web/client/src/App.tsx`; `apps/admin_web/client/src/lib/admin-api.ts`; `apps/admin_web/server/admin-auth.ts`; `apps/admin_web/server/index.ts`; `docs/evidence/TASK_048_ADMIN_LOGIN_EVIDENCE.md`; `artifacts/task048_admin_login_result.json` | لا DB/API/RLS/صلاحيات جديدة؛ يستخدم `adminApi.session/login/logout/changePassword` ومسارات `/api/admin/auth/*` القائمة، و`signInAdmin` و`getAdminSession` و`requireAdmin` مع `admin_users`/`admin_roles` وSupabase Production خلف الخادم المحلي | دُققت بوابة Admin المستقلة: تحقق جلسة عند البدء، نموذج بريد/كلمة مرور، خطأ عربي داخل `role=alert`، حاجز تغيير كلمة المرور الأولي، تمرير إلى `Home` بعد الجلسة، logout ومسح الكوكيز، وحماية same-origin للطلبات mutation. لم يُدمج مسار العميل أو OTP أو OAuth، ولم يتطلب السلوك القائم تعديلًا إنتاجيًا | loading/session gate، login form، validation، invalid credentials، no session، forced password change، password error، authenticated pass-through، logout، CSRF boundary، permission boundary، وlocal service unavailable/timeout عبر تطبيع أخطاء العميل | `pnpm test -- client/src/lib/admin-api.test.ts client/src/lib/ui-consistency.test.ts server/admin-auth.test.ts server/admin-csrf.test.ts server/admin-security.test.ts`: PASS (5 ملفات). `pnpm check`: PASS. `pnpm build`: PASS. HTTP probes المحلية: health 200، session بلا cookie 401، login فارغ 400، login غير صالح 401، password بلا cookie 401، logout بلا cookie 200. لم يُختبر login إيجابي بحساب حقيقي | لا golden بصري قابل للتشغيل: أصول `048_admin_login.md/.png` غير موجودة في checkout أو شجرة GitHub المتاحة، لذلك لم يُنشأ baseline بديل ولم تُعلن مطابقة. فحص HTTP للواجهة أعاد 200، عنوان `عسلكم — لوحة الإدارة` و`#root` موجودان؛ لا يُعد ذلك قبولًا بصريًا | GAP-022: فقدان Markdown/PNG المرجعيين يمنع المقارنة البصرية وإعادة قراءة العقد. تبقى الجلسة الحية الإيجابية وrefresh وتغيير كلمة المرور الحقيقي غير متحققين دون اعتماد آمن؛ لا تغيير DB/API/RLS/الصلاحيات | PASS مع حدود موثقة — المصدر والحدود السلبية والبناء ناجحة، والجلسة الحية والمقارنة البصرية غير مثبتتين |
| 049 | `screens/049_admin_shell.png` + `explanations/049_admin_shell.md` | الهيكل العام والقائمة الجانبية — إدارة — سطح مكتب واقعي — مرجع 23.2–23.3 | `apps/admin_web/client/src/App.tsx`; `apps/admin_web/client/src/pages/Home.tsx`; `apps/admin_web/client/src/lib/admin-api.ts`; `docs/evidence/TASK_049_ADMIN_SHELL_EVIDENCE.md`; `artifacts/task049_admin_shell_result.json` | لا DB/API/RLS/صلاحيات جديدة؛ التنقل يعتمد `ViewKey` و`navItems` و`setActiveView` داخل Admin client، والبيانات تمر عبر `adminApi` القائمة خلف `AdminAuthGate` | دُقق shell موحد مستقل للإدارة: aside ثابت يمينًا على سطح المكتب، Sheet يمينًا للشاشات الأصغر، خمس مجموعات و15 وجهة فعلية، إغلاق Sheet بعد الاختيار، عنوان مسار حالي، زر إشعارات، خروج، وحالة مصدر. القائمة تستخدم `overflow-y-auto` و`overscroll-contain` لمنع قص العناصر، وكل عنصر يربط انتقاله بـ`setActiveView` ومكوّن محتوى حقيقي؛ لم تُضف أزرار أو مسارات شكلية | عرض aside وmobile sheet، التنقل بين ViewKeys، المجموعة/العنصر النشط، overflow/scroll، الإغلاق، الحالة الفارغة/التحميل/الخطأ وإعادة المحاولة داخل المحتوى القائم، وauth boundary قبل Home | `pnpm test`: PASS — 8 ملفات اختبار. `pnpm check`: PASS. `pnpm build`: PASS؛ تحذير chunk أكبر من 500 kB بقي تحذير أداء. الجرد الثابت: 15 عنصر تنقل و5 مجموعات وعلامات testid/overflow/sheet موجودة. لا screenshot browser runtime مُدّعى | لا golden بصري: `049_admin_shell.md/.png` غير موجودين في checkout أو شجرة GitHub المتاحة، لذلك لم يُصنع baseline بديل ولم تُعلن مطابقة. لا يُعد الجرد النصي بديلًا عن المرجع المكتبي 1600×1000 | GAP-023: فقدان Markdown/PNG يمنع إعادة قراءة العقد والمقارنة البصرية. لا تغيير DB/API/RLS/الصلاحيات ولا ادعاء مزامنة أو production؛ فحص browser الإيجابي مؤجل لبيئة قابلة للوصول للخدمة المحلية | PASS مع حدود موثقة — المصدر والتنقل والبناء والاختبارات ناجحة، والمقارنة البصرية محجوبة بأصول مفقودة |
| 050 | `screens/050_admin_overview.png` + `explanations/050_admin_overview.md` | نظرة عامة الإدارة — إدارة — سطح مكتب واقعي — مرجع 23.4 | `apps/admin_web/client/src/pages/Home.tsx`; `apps/admin_web/client/src/lib/admin-api.ts`; `docs/evidence/TASK_050_ADMIN_OVERVIEW_EVIDENCE.md`; `artifacts/task050_admin_overview_result.json` | لا DB/API/RLS/صلاحيات جديدة؛ الموجز يستخدم `adminApi.overview` والحالات المحلية `LoadState`، والتصدير يستخدم `downloadSnapshot` القائم عند وجود بيانات صالحة | حُصّنت نظرة عامة الإدارة بحيث تعرض `ErrorState` وزر «إعادة المحاولة» عند فشل `overview` بدل شارة نجاح ومقاييس غير موثقة. عُطّل «تصدير الموجز» أثناء التحميل أو عند غياب البيانات أو الخطأ، وبقي التصدير الحقيقي متاحًا بعد وصول بيانات الموجز. لم تُضف بيانات Demo أو endpoint جديد، وبقيت مصادر الطلبات والمنتجات مستقلة | loading، data، error/retry، export guard، بطاقات المقاييس المصدرية، banner حالة المصدر، وبقاء حالات الطلبات والمنتجات | `pnpm test`: PASS — 8 ملفات. `pnpm check`: PASS. `pnpm build`: PASS؛ تحذير chunk أكبر من 500 kB تحذير أداء فقط. مراجعة المصدر أثبتت أن الخطأ يحجب banner الأخضر والتصدير محمي بـloading/data/error | لا golden بصري: `050_admin_overview.md/.png` غير موجودين في checkout أو شجرة GitHub المتاحة، لذلك لم يُنشأ baseline بديل ولم تُعلن مطابقة. لا screenshot browser runtime مُدّعى | GAP-024: فقدان Markdown/PNG يمنع المقارنة البصرية وإعادة قراءة عقد حالات الموجز تفصيليًا. لا تغيير DB/API/RLS/الصلاحيات ولا ادعاء production أو مزامنة خارج حدود المصدر | PASS مع حدود موثقة — الإصلاح الوظيفي والبناء والاختبارات ناجحة، والمقارنة البصرية محجوبة بأصول مفقودة |
| 051 | `screens/051_admin_catalog.png` + `explanations/051_admin_catalog.md` | إدارة الكتالوج — إدارة — سطح مكتب واقعي — مرجع 23.5 | `apps/admin_web/client/src/pages/Home.tsx`; `apps/admin_web/client/src/components/ProductCreationPanel.tsx`; `apps/admin_web/client/src/components/ProductEditPanel.tsx`; `apps/admin_web/client/src/lib/admin-api.ts`; `docs/evidence/TASK_051_ADMIN_CATALOG_EVIDENCE.md`; `artifacts/task051_admin_catalog_result.json` | لا DB/API/RLS/صلاحيات جديدة؛ القائمة والمحرران يستخدمان `adminApi.products/taxonomy/regions/uploadPublicImage/createProduct/updateProduct/deleteProduct` القائمة، مع حقول `metadata` والصور الحالية | دُققت قائمة الكتالوج مع loading/error/empty/retry والبحث والتفاصيل والتعديل والحذف الحقيقي. حُصنت لوحة إنشاء المنتج بحيث تحتفظ بأخطاء قراءة `taxonomy/regions` وتعرض تنبيهًا عربيًا وزر «إعادة المحاولة» وتعطل الاختيارات حتى عودة المصدر، وحُصن محرر التعديل بالطريقة نفسها لمصدر التصنيف. بقيت التحققات والرفع والحفظ عبر المسارات القائمة دون بيانات Demo أو endpoint جديد | قائمة المنتجات، البحث، empty/error/retry، إنشاء مسودة، تعديل منتج، التحقق من الاسم والسعر وmetadata، التصنيف والمناطق، رفع الصور والتحقق من النوع/الحجم، إزالة الصور عند الحفظ، وتعطيل التكرار أثناء الرفع/الحفظ | `pnpm test`: PASS — 8 ملفات. `pnpm check`: PASS. `pnpm build`: PASS؛ تحذير chunk أكبر من 500 kB تحذير أداء فقط. مراجعة المصدر أثبتت وجود reference error/retry في الإنشاء والتعديل وعدم فقدان مسارات create/update/upload/delete | لا golden بصري: `051_admin_catalog.md/.png` غير موجودان في checkout أو شجرة GitHub المتاحة، لذلك لم يُنشأ baseline بديل ولم تُعلن مطابقة. لا screenshot browser runtime مُدّعى | GAP-025: فقدان Markdown/PNG يمنع المقارنة البصرية وإعادة قراءة عقد الكتالوج تفصيليًا. لا تغيير DB/API/RLS/الصلاحيات ولا ادعاء write في Production خلال المهمة | PASS مع حدود موثقة — التعديل الوظيفي المحدود والبناء والاختبارات ناجحة، والمقارنة البصرية محجوبة بأصول مفقودة |
| 052 | `screens/052_admin_product_create.png` + `explanations/052_admin_product_create.md` | إنشاء منتج في الإدارة — إدارة — سطح مكتب واقعي — مرجع 23.6 | `apps/admin_web/client/src/components/ProductCreationPanel.tsx`; `apps/admin_web/client/src/lib/admin-api.ts`; `apps/admin_web/server/index.ts`; `apps/admin_web/server/admin-data.ts`; `docs/evidence/TASK_052_ADMIN_PRODUCT_CREATE_EVIDENCE.md`; `artifacts/task052_admin_product_create_result.json` | لا DB/API/RLS/صلاحيات جديدة؛ الإنشاء يستخدم `adminApi.createProduct` و`taxonomy/regions/uploadPublicImage` ومسار الخادم `createProduct` و`product.write` وسجل التدقيق الحالي | دُقق نموذج إنشاء المنتج بجميع حقوله المصدرية: المتجر والتصنيف والهوية والوصف والنوع والجودة والسعر والعملة والوزن والمصدر والمناطق والبيانات الوصفية والتوفر والتوصيل والاستلام والصور. التحقق يمنع الحقول الأساسية والسعر غير الصالح، و`textMetadata` يبني metadata منظمًا، ورفع الصور يتحقق من النوع والحجم ثم يستخدم Storage endpoint، والحفظ ينشئ مسودة فعلية مع الصور وسجل التدقيق وفق العقد القائم. لا write حي في هذه المهمة | loading/error/retry للمراجع كما حُصنت في TASK 051، empty الحقول، validation، اختيار المتجر/التصنيف/المناطق، metadata، رفع الصور، إنشاء المسودة، تعطيل التكرار والحالات المصدرية | `pnpm test`: PASS — 8 ملفات. `pnpm check`: PASS. `pnpm build`: PASS؛ تحذير chunk أكبر من 500 kB تحذير أداء فقط. مراجعة `admin-data.ts` أثبتت `product.write` وcreate insert والصور والتدقيق | لا golden بصري: `052_admin_product_create.md/.png` غير موجودان في checkout أو شجرة GitHub المتاحة، لذلك لم يُنشأ baseline بديل ولم تُعلن مطابقة. لا write أو upload Production حي مُدّعى | GAP-026: فقدان Markdown/PNG يمنع المقارنة البصرية وإعادة قراءة عقد الإنشاء تفصيليًا. لا تغيير DB/API/RLS/الصلاحيات ولا ادعاء مزامنة أو production write | PASS مع حدود موثقة — مسار الإنشاء المصدرّي والتحقق والبناء والاختبارات ناجحة، والمقارنة البصرية والـwrite الحي خارج الإثبات |
| 053 | `screens/053_admin_product_edit.png` + `explanations/053_admin_product_edit.md` | تعديل منتج في الإدارة — إدارة — سطح مكتب واقعي — مرجع 23.7 | `apps/admin_web/client/src/components/ProductEditPanel.tsx`; `apps/admin_web/client/src/lib/admin-api.ts`; `apps/admin_web/server/admin-data.ts`; `docs/evidence/TASK_053_ADMIN_PRODUCT_EDIT_EVIDENCE.md`; `artifacts/task053_admin_product_edit_result.json` | لا DB/API/RLS/صلاحيات جديدة؛ التعديل يستخدم `adminApi.updateProduct` و`taxonomy/uploadPublicImage` ومسار الخادم `updateProduct` و`product.write` وسجل التدقيق الحالي | دُقق محرر تعديل المنتج للهوية والوصف والتصنيف والنوع والجودة والسعر والعملة والحالة وmetadata والصور. التحقق يمنع الاسم والسعر غير الصالحين وmetadata غير الكائن، والحفظ يعطل التكرار. عند تغيير الصور يرسل `imageUrls` صراحة ليحذف الخادم القائمة القديمة ويعيد إدخال الصور المرتبة، ثم يسجل `product.update`؛ الإغلاق لا يحفظ، و`onSaved` يحدث القائمة والموجز. إضافة retry التصنيف وحالته العربية محفوظة من TASK 051 | loading/error/retry للتصنيف، validation، JSON metadata، تعديل الحقول، استبدال الصور، حذف صورة من القائمة، الحفظ، الإلغاء، الصلاحية وسجل التدقيق | `pnpm test`: PASS — 8 ملفات. `pnpm check`: PASS. `pnpm build`: PASS؛ تحذير chunk أكبر من 500 kB تحذير أداء فقط. مراجعة `admin-data.ts` أثبتت patch منضبطًا و`product.write` وaudit boundary | لا golden بصري: `053_admin_product_edit.md/.png` غير موجودان في checkout أو شجرة GitHub المتاحة، لذلك لم يُنشأ baseline بديل ولم تُعلن مطابقة. لا write أو upload Production حي مُدّعى | GAP-027: فقدان Markdown/PNG يمنع المقارنة البصرية وإعادة قراءة عقد التعديل تفصيليًا. لا تغيير DB/API/RLS/الصلاحيات ولا ادعاء مزامنة أو production write | PASS مع حدود موثقة — مسار التعديل المصدرّي والبناء والاختبارات ناجحة، والمقارنة البصرية والـwrite الحي خارج الإثبات |
| 054 | `screens/054_admin_stores.png` + `explanations/054_admin_stores.md` | إدارة المتاجر — إدارة — سطح مكتب واقعي — مرجع 23.8 | `apps/admin_web/client/src/pages/Home.tsx`; `apps/admin_web/client/src/lib/admin-api.ts`; `apps/admin_web/server/admin-data.ts`; `docs/evidence/TASK_054_ADMIN_STORES_EVIDENCE.md`; `artifacts/task054_admin_stores_result.json` | لا DB/API/RLS/صلاحيات جديدة؛ قائمة المتاجر تستخدم `adminApi.stores`، والأفعال تمر عبر `moderateStore` و`deleteStore` والصلاحيات وRPC `admin_moderate_store` وسجل التدقيق القائم | دُققت قائمة المتاجر بحالات loading/error/empty/retry، fallback آمن للشعار/الغلاف، تفاصيل Sheet، ومؤشر الحالة. أفعال الاعتماد والرفض والتعليق وإعادة التفعيل والحذف مشروطة بالحالة ومحمية بتأكيد، وبعدها يحدث المصدر والمنتجات والموجز. لم تُخلط حالة فتح المتجر مع توثيق Pro، ولم تُضف أفعال شكلية أو write جديد | قائمة المتاجر، صورة fallback، التفاصيل، الحالات pending/active/rejected/suspended/paused، approve/reject/suspend/reactivate، delete، confirmation، toast error/success، permission/RPC/audit boundary | `pnpm test`: PASS — 8 ملفات. `pnpm check`: PASS. `pnpm build`: PASS؛ تحذير chunk أكبر من 500 kB تحذير أداء فقط. مراجعة source أثبتت ترجمة action إلى permission وRPC وإشعار/مزامنة payload وفق العقد القائم | لا golden بصري: `054_admin_stores.md/.png` غير موجودان في checkout أو شجرة GitHub المتاحة، لذلك لم يُنشأ baseline بديل ولم تُعلن مطابقة. لا approve/reject/delete حي مُدّعى | GAP-028: فقدان Markdown/PNG يمنع المقارنة البصرية وإعادة قراءة عقد المتاجر تفصيليًا. لا تغيير DB/API/RLS/الصلاحيات ولا ادعاء مزامنة إنتاجية أو write حي | PASS مع حدود موثقة — المصدر والأفعال والبناء والاختبارات ناجحة، والمقارنة البصرية والـwrite الحي خارج الإثبات |
| 055 | `screens/055_admin_store_detail.png` + `explanations/055_admin_store_detail.md` | تفاصيل المتجر في الإدارة — إدارة — سطح مكتب واقعي — مرجع 23.9 | `apps/admin_web/client/src/pages/Home.tsx`; `apps/admin_web/client/src/components/AdminVisuals.tsx`; `docs/evidence/TASK_055_ADMIN_STORE_DETAIL_EVIDENCE.md`; `artifacts/task055_admin_store_detail_result.json` | لا DB/API/RLS/صلاحيات جديدة؛ التفاصيل تعتمد `selectedStore` الناتج من `adminApi.stores` والعقد `StoreRow`، والصور تمر عبر `AdminSafeImage` المحلي | حُصنت Sheet تفاصيل المتجر باستخدام fallback آمن للغلاف والشعار بدل صورة مكسورة، وأضيفت الحالة والاعتماد ومعرّفات المتجر والتاجر والمنطقة من المصدر الحالي. بقي الهاتف موصوفًا بصدق بأنه غير موجود في عقد القائمة، وبقيت أفعال الفتح والإغلاق والـmoderation منفصلة وحقيقية | Sheet open/close، cover/logo absent/broken fallback، status، approval، source IDs، حدود الهاتف، وبقاء أفعال القائمة دون تغيير | `pnpm test`: PASS — 8 ملفات. `pnpm check`: PASS. `pnpm build`: PASS؛ تحذير chunk أكبر من 500 kB تحذير أداء فقط. مراجعة المصدر أثبتت أن الصور المكسورة لا تظهر كـbroken image وأن الحقول المضافة مصدرية | لا golden بصري: `055_admin_store_detail.md/.png` غير موجودان في checkout أو شجرة GitHub المتاحة، لذلك لم يُنشأ baseline بديل ولم تُعلن مطابقة. لا قراءة أو تعديل Store Production حي مُدّعى | GAP-029: فقدان Markdown/PNG، وغياب الهاتف من `StoreRow` يمنع المقارنة والحقول غير المصدرية. لا تغيير DB/API/RLS/الصلاحيات ولا ادعاء مزامنة أو production | PASS مع حدود موثقة — الإصلاح الوظيفي المحدود والبناء والاختبارات ناجحة، والمقارنة البصرية والحقول غير الموجودة خارج الإثبات |
| 056 | `screens/056_admin_requests.png` + `explanations/056_admin_requests.md` | طلبات التواصل وتفاصيلها — إدارة — سطح مكتب واقعي — مرجع 23.10 | `apps/admin_web/client/src/pages/Home.tsx`; `apps/admin_web/client/src/lib/admin-api.ts`; `apps/admin_web/server/admin-data.ts`; `docs/evidence/TASK_056_ADMIN_REQUESTS_EVIDENCE.md`; `artifacts/task056_admin_requests_result.json` | لا DB/API/RLS/صلاحيات جديدة؛ الطلبات تستخدم `adminApi.requests` و`answerRequest` ومسارات `request_messages` و`requests` وسجل التدقيق القائمة | دُققت قائمة طلبات التواصل وSheet التفاصيل والرد الإداري الحقيقي. أضيفت حالة `replying` وحماية submit وتعطيل الزر عند الفراغ أو أثناء الإرسال مع نص «جارٍ إرسال الرد…»، وبقي النجاح يحدث الطلبات ويغلق التفاصيل، بينما يبقي الخطأ السياق ظاهرًا. لم تُضف قناة أو endpoint أو نجاح شكلي | loading/error/empty/refresh، فتح/إغلاق التفاصيل، عرض الموضوع والوصف والحالة، validation النص، منع الإرسال المكرر، answer path، answered refresh، error retention، والحدود الأمنية القائمة | `pnpm check`: PASS. `pnpm test`: PASS — 8 ملفات. `pnpm build`: PASS؛ تحذير chunk أكبر من 500 kB تحذير أداء فقط. ظهرت محاولة أولى فاشلة بسبب typo إدخال في `overview`، شُخّصت وأُصلحت ثم مرّت إعادة الاختبار كاملة | لا golden بصري: `056_admin_requests.md/.png` غير موجودان في checkout أو شجرة GitHub المتاحة، لذلك لم يُنشأ baseline بديل ولم تُعلن مطابقة. لا إرسال رسالة Production حي مُدّعى | GAP-030: فقدان Markdown/PNG يمنع المقارنة البصرية وإعادة قراءة عقد التفاصيل تفصيليًا. لا تغيير DB/API/RLS/الصلاحيات ولا ادعاء وصول إلى هاتف أو مزامنة لحظية | PASS مع حدود موثقة — إصلاح منع التكرار والبناء والاختبارات ناجحة، والمقارنة البصرية والوصول الحي خارج الإثبات |
| 057 | `screens/057_admin_merchant_requests.png` + `explanations/057_admin_merchant_requests.md` | طلبات التجار — إدارة — سطح مكتب واقعي — مرجع 23.11 | `apps/admin_web/client/src/components/AdminMerchantApplications.tsx`; `apps/admin_web/client/src/lib/admin-api.ts`; `apps/admin_web/server/admin-data.ts`; `docs/evidence/TASK_057_ADMIN_MERCHANT_REQUESTS_EVIDENCE.md`; `artifacts/task057_admin_merchant_requests_result.json` | لا DB/API/RLS/صلاحيات جديدة؛ المراجعة تستخدم `adminApi.reviewMerchantApplication` والمسار القائم للمزامنة والإشعار والصلاحيات وسجل التدقيق | دُققت قائمة طلبات التجار مع loading/error/empty/refresh والصورة الآمنة ومعلومات المتقدم والملاحظة. حُصنت أفعال تفعيل المتجر وطلب المعلومات والرفض بحالة `reviewingId` وguard وتأكيد عربي وتعطيل أثناء الحفظ، وبقيت الحالة المعتمدة بلا أفعال مكررة ونجاح القرار يعيد التحميل | list states، image fallback، status، notes، approve/reject/needs_more_info، confirmation، منع النقر المكرر، error/success، refresh، وبقاء Pro/التفعيل ضمن العقد الإداري القائم | `pnpm test`: PASS — 8 ملفات. `pnpm check`: PASS. `pnpm build`: PASS؛ تحذير chunk أكبر من 500 kB تحذير أداء فقط. مراجعة source أثبتت مسار review الحقيقي وحدود الإشعار/المزامنة | لا golden بصري: `057_admin_merchant_requests.md/.png` غير موجودان في checkout أو شجرة GitHub المتاحة، لذلك لم يُنشأ baseline بديل ولم تُعلن مطابقة. لا اعتماد طلب تاجر حي أو إشعار Production مُدّعى | GAP-031: فقدان Markdown/PNG يمنع المقارنة البصرية وإعادة قراءة عقد طلبات التجار تفصيليًا. لا تغيير DB/API/RLS/الصلاحيات ولا ادعاء تفعيل أو مزامنة بين جهازين | PASS مع حدود موثقة — تحصين قرارات المراجعة والبناء والاختبارات ناجح، والمقارنة البصرية والاعتماد الحي خارج الإثبات |
| 058 | `screens/058_admin_verification.png` + `explanations/058_admin_verification.md` | توثيق المتاجر Pro — إدارة — سطح مكتب واقعي — مرجع 23.12 | `apps/admin_web/client/src/components/AdminStoreVerification.tsx`; `apps/admin_web/client/src/lib/admin-api.ts`; `apps/admin_web/server/admin-data.ts`; `docs/evidence/TASK_058_ADMIN_VERIFICATION_EVIDENCE.md`; `artifacts/task058_admin_verification_result.json` | لا DB/API/RLS/صلاحيات جديدة؛ التوثيق يستخدم `adminApi.storeVerificationRequests/storeVerificationRequest/reviewStoreVerification/reconcileStoreVerificationPayment` وRPCs والصلاحيات وسجل التدقيق القائمة | دُققت قائمة طلبات التوثيق وتفاصيل المستندات وsigned URLs وتسوية الدفع المحلي وقرارات approve/reject/needs_more_info/revoke. فُصل خطأ قراءة المستندات عن خطأ القائمة بإضافة `detailLoading` و`detailError` وتنبيه عربي وزر retry يعيد قراءة الطلب المحدد، مع بقاء نص الدفع اليدوي غير المالي وحدود الصلاحيات كما هي | list loading/error/empty/refresh، detail loading/error/retry، مستندات signed، paid/waived/failed/refunded، review actions، `verification.read_sensitive/review/approve/reject`، audit، وعدم فتح دفع البطاقة المجمد | `pnpm check`: PASS. `pnpm test`: PASS — 8 ملفات. `pnpm build`: PASS؛ تحذير chunk أكبر من 500 kB تحذير أداء فقط. لا write أو تسوية Production حية | لا golden بصري: `058_admin_verification.md/.png` غير موجودان في checkout أو شجرة GitHub المتاحة، لذلك لم يُنشأ baseline بديل ولم تُعلن مطابقة. لا مستند أو دفع حي مُدّعى | GAP-032: فقدان Markdown/PNG يمنع المقارنة البصرية وإعادة قراءة عقد التوثيق تفصيليًا. لا تغيير DB/API/RLS/الصلاحيات ولا ادعاء تفعيل تلقائي أو دفع بطاقة أو مزامنة حية | PASS مع حدود موثقة — تحصين حالات تفاصيل المستندات والبناء والاختبارات ناجح، والمقارنة البصرية وتسوية/اعتماد Production خارج الإثبات |
| 059 | `screens/059_admin_plans_campaign.png` + `explanations/059_admin_plans_campaign.md` | الخطط والحملة والدفع المحلي — إدارة — سطح مكتب واقعي — مرجع 23.13 | `apps/admin_web/client/src/components/AdminPlansPanel.tsx`; `apps/admin_web/client/src/lib/admin-api.ts`; `apps/admin_web/server/admin-data.ts`; `docs/evidence/TASK_059_ADMIN_PLANS_CAMPAIGN_EVIDENCE.md`; `artifacts/task059_admin_plans_campaign_result.json` | لا DB/API/RLS/صلاحيات جديدة؛ اللوحة تستخدم عقود الخطط والحملة والمدفوعات والاشتراكات وطلبات التصميم والحوالة والمستخدمين القائمة، والبطاقة مصممة ومجمّدة خادميًا | دُققت بطاقات الخطط وحساب الخصم من الحملة وإيقاف/تفعيل الحملة وإعدادات الحوالة وطلبات الدفع وفتح السند والتأكيد/الرفض/الرد والتفعيل اليدوي والاشتراكات وطلبات التصميم. حُصنت القراءة التجميعية بحالة `error` مستمرة وتنبيه retry حتى لا تعرض اللوحة حالات فارغة مضللة عند فشل أحد المصادر، مع بقاء الأسعار من Production وعدم إضافة Demo | aggregate loading/error/retry، plan pricing/discount، campaign active/inactive، local transfer، payment proof/reconcile، manual activation، subscription state، design request state، وبقاء البطاقة المجمدة | `pnpm check`: PASS. `pnpm test`: PASS — 8 ملفات. `pnpm build`: PASS؛ تحذير chunk أكبر من 500 kB تحذير أداء فقط. مراجعة المصدر أثبتت عقود التفعيل والإشعار والخصم والدفع المحلي القائمة | لا golden بصري: `059_admin_plans_campaign.md/.png` غير موجودان في checkout أو شجرة GitHub المتاحة، لذلك لم يُنشأ baseline بديل ولم تُعلن مطابقة. لا تعديل حملة/خطة/حوالة/اشتراك Production حي مُدّعى | GAP-033: فقدان Markdown/PNG يمنع المقارنة البصرية وإعادة قراءة العقد تفصيليًا. لا تغيير DB/API/RLS/الصلاحيات ولا ادعاء دفع بطاقة أو تفعيل تلقائي أو مزامنة حية | PASS مع حدود موثقة — تحصين خطأ المصدر والبناء والاختبارات ناجح، والمقارنة البصرية والتعديلات الحية خارج الإثبات |
| 060 | `screens/060_admin_banners.png` + `explanations/060_admin_banners.md` | البنرات الحية — إدارة — سطح مكتب واقعي — مرجع 23.14 | `apps/admin_web/client/src/components/AdminOperations.tsx`; `apps/admin_web/client/src/lib/admin-api.ts`; `apps/admin_web/server/admin-data.ts`; `docs/evidence/TASK_060_ADMIN_BANNERS_EVIDENCE.md`; `artifacts/task060_admin_banners_result.json` | لا DB/API/RLS/صلاحيات جديدة؛ البنرات تستخدم `adminApi.banners/createBanner/updateBanner/deleteBanner/uploadPublicImage` ومسار الجدولة والتدقيق القائم | دُققت قائمة البنرات وحالات loading/error/empty/retry، إنشاء المسودة، رفع ومعاينة الصورة، جدولة البداية والنهاية، النشر والإيقاف والحذف. حُصنت أفعال البانر بـ`bannerBusyId` ومنع التكرار، وأضيف تأكيد عربي للنشر/الإيقاف، وتحقق MIME صريح لـPNG/JPEG/WebP قبل الرفع، مع بقاء `AdminSafeImage` وجدولة المصدر كما هما | list states، draft validation، image upload/preview، MIME/size guard، schedule validation، publish/stop confirmation، delete confirmation، per-banner busy state، safe image fallback | `pnpm check`: PASS. `pnpm test`: PASS — 8 ملفات. `pnpm build`: PASS؛ تحذير chunk أكبر من 500 kB تحذير أداء فقط. مراجعة المصدر أثبتت مسارات CRUD والجدولة والصور والتدقيق القائمة | لا golden بصري: `060_admin_banners.md/.png` غير موجودان في checkout أو شجرة GitHub المتاحة، لذلك لم يُنشأ baseline بديل ولم تُعلن مطابقة. لا إنشاء/نشر/حذف Production حي مُدّعى | GAP-034: فقدان Markdown/PNG يمنع المقارنة البصرية وإعادة قراءة العقد تفصيليًا. لا تغيير DB/API/RLS/الصلاحيات ولا ادعاء ظهور البانر في العميل أو مزامنة حية | PASS مع حدود موثقة — حماية الأفعال والتحقق من MIME والبناء والاختبارات ناجحة، والمقارنة البصرية والكتابة الحية خارج الإثبات |
| 061 | `screens/061_admin_taxonomy.png` + `explanations/061_admin_taxonomy.md` | التصنيفات والأنواع — إدارة — سطح مكتب واقعي — مرجع 23.15 | `apps/admin_web/client/src/components/AdminOperations.tsx`; `apps/admin_web/client/src/lib/admin-api.ts`; `apps/admin_web/server/admin-data.ts`; `docs/evidence/TASK_061_ADMIN_TAXONOMY_EVIDENCE.md`; `artifacts/task061_admin_taxonomy_result.json` | لا DB/API/RLS/صلاحيات جديدة؛ النوع والتصنيف يستخدمان `adminApi.taxonomy/categories/upsertTaxonomy/upsertCategory` ومسار validation/audit القائم | دُققت لوحة الأنواع والتصنيفات بحالات loading/error/retry/empty وأزرار التعديل ونماذج upsert. أضيفت حالتا `savingTaxonomy` و`savingCategory` وتعطيل زر كل نموذج أثناء الحفظ لمنع الإرسال المكرر، مع بقاء القراءة والكتابة عبر العقد القائمة وعدم إدخال بيانات افتراضية | loading، error/retry، empty، edit، create، validation المصدرية، duplicate-submit guard، upsert success/error | `pnpm check`: PASS. `pnpm test`: PASS — 8 ملفات. `pnpm build`: PASS؛ تحذير chunk أكبر من 500 kB تحذير أداء فقط. لم تُنفذ كتابة Production حية | لا golden بصري: `061_admin_taxonomy.md/.png` غير موجودان في checkout أو شجرة GitHub المتاحة، لذلك لم يُنشأ baseline بديل ولم تُعلن مطابقة | GAP-035: فقدان Markdown/PNG يمنع المقارنة البصرية وإعادة قراءة العقد تفصيليًا. لا تغيير DB/API/RLS/الصلاحيات ولا ادعاء مزامنة حية | PASS مع حدود موثقة — حماية حفظ النماذج والبناء والاختبارات ناجحة، والمقارنة البصرية والكتابة الحية خارج الإثبات |
| 062 | `screens/062_admin_logistics.png` + `explanations/062_admin_logistics.md` | التوصيل ونقاط الاستلام — إدارة — سطح مكتب واقعي — مرجع 23.16 | `apps/admin_web/client/src/components/AdminOperations.tsx`; `apps/admin_web/client/src/lib/admin-api.ts`; `apps/admin_web/server/admin-data.ts`; `docs/evidence/TASK_062_ADMIN_LOGISTICS_EVIDENCE.md`; `artifacts/task062_admin_logistics_result.json` | لا DB/API/RLS/صلاحيات جديدة؛ اللوجستيات تستخدم `adminApi.deliveryMethods/regions/storeLogistics/upsertDeliveryOption/upsertPickupLocation/deleteDeliveryOption/deletePickupLocation` والعقود القائمة | دُققت مراجع طرق التوصيل والمناطق وإعدادات المتجر وخياري التوصيل ونقاط الاستلام مع تسلسل المحافظة/المديرية. أضيفت حالات `error/storeError` مع retry بدل ترك الواجهة فارغة بعد فشل القراءة، وحالة `removingId` لمنع حذف السجل مكررًا، مع بقاء حارس الحفظ والتأكيد والعقود المصدرية | reference/store loading، error/retry، no store، empty delivery/pickup، cascading regions، save guard، delete confirmation/guard، مصدر الرسوم والعملة والمدة والعنوان | `pnpm check`: PASS. `pnpm test`: PASS — 8 ملفات. `pnpm build`: PASS؛ تحذير chunk أكبر من 500 kB تحذير أداء فقط. لم تُنفذ كتابة أو حذف Production حي | لا golden بصري: `062_admin_logistics.md/.png` غير موجودان في checkout أو شجرة GitHub المتاحة، لذلك لم يُنشأ baseline بديل ولم تُعلن مطابقة | GAP-036: فقدان Markdown/PNG يمنع المقارنة البصرية وإعادة قراءة العقد تفصيليًا. لا تغيير DB/API/RLS/الصلاحيات ولا ادعاء وصول جديد للعميل أو مزامنة حية | PASS مع حدود موثقة — حالات المصدر وحارس الحذف والبناء والاختبارات ناجحة، والمقارنة البصرية والكتابة الحية خارج الإثبات |
| 063 | `screens/063_admin_managers.png` + `explanations/063_admin_managers.md` | المديرون والصلاحيات — إدارة — سطح مكتب واقعي — مرجع 23.17 | `apps/admin_web/client/src/components/AdminGovernance.tsx`; `apps/admin_web/client/src/lib/admin-api.ts`; `apps/admin_web/server/admin-data.ts`; `docs/evidence/TASK_063_ADMIN_MANAGERS_EVIDENCE.md`; `artifacts/task063_admin_managers_result.json` | لا DB/API/RLS/صلاحيات جديدة؛ العضويات تستخدم `adminApi.adminUsers/updateAdminUser` وشرط `admin.manage` ومسار audit القائم | دُققت قائمة المديرين والأدوار وحالات العضوية. أضيف تأكيد عربي للتفعيل/التعطيل، وحالة `updatingId` لتعطيل الإجراء أثناء الطلب، وزر إعادة محاولة واضح عند فشل القراءة، مع بقاء التفويض ومنع تعطيل الهوية الحالية في الخادم | loading، error/retry، empty، active/disabled، confirmation، duplicate-action guard، success/error، role display | `pnpm check`: PASS. `pnpm test`: PASS — 8 ملفات. `pnpm build`: PASS؛ تحذير chunk أكبر من 500 kB تحذير أداء فقط. لم تُجرَ عملية عضوية Production حية | لا golden بصري: `063_admin_managers.md/.png` غير موجودان في checkout أو شجرة GitHub المتاحة، لذلك لم يُنشأ baseline بديل ولم تُعلن مطابقة | GAP-037: فقدان Markdown/PNG يمنع المقارنة البصرية وإعادة قراءة العقد تفصيليًا. لا تغيير DB/API/RLS/الصلاحيات ولا ادعاء تعديل RBAC حي | PASS مع حدود موثقة — حماية الأفعال والبناء والاختبارات ناجحة، والمقارنة البصرية وأفعال العضوية الحية خارج الإثبات |
| 064 | `screens/064_admin_audit_log.png` + `explanations/064_admin_audit_log.md` | سجل التدقيق — إدارة — سطح مكتب واقعي — مرجع 23.18 | `apps/admin_web/client/src/components/AdminGovernance.tsx`; `apps/admin_web/client/src/lib/admin-api.ts`; `apps/admin_web/server/admin-data.ts`; `docs/evidence/TASK_064_ADMIN_AUDIT_LOG_EVIDENCE.md`; `artifacts/task064_admin_audit_log_result.json` | لا DB/API/RLS/صلاحيات جديدة؛ السجل يستخدم `adminApi.auditLogs` وlabels والتدقيق القائم | دُققت قراءة سجل التدقيق وعرض الفعل والكيان والزمن. أضيف retry واضح داخل حالة الخطأ، ووسم وصول لتحديث السجل، وعرض `entity_id` و`actor_user_id` عند توفرهما دون اختراع قيم أو تغيير مسار البيانات | loading، error/retry، empty، action/entity labels، entity id، actor id، timestamp | `pnpm check`: PASS. `pnpm test`: PASS — 8 ملفات. `pnpm build`: PASS؛ تحذير chunk أكبر من 500 kB تحذير أداء فقط. لم تُجرَ كتابة أو قراءة Production حية | لا golden بصري: `064_admin_audit_log.md/.png` غير موجودان في checkout أو شجرة GitHub المتاحة، لذلك لم يُنشأ baseline بديل ولم تُعلن مطابقة | GAP-038: فقدان Markdown/PNG يمنع المقارنة البصرية وإعادة قراءة العقد تفصيليًا. لا تغيير DB/API/RLS/الصلاحيات ولا ادعاء اكتمال retention أو تصدير حي | PASS مع حدود موثقة — retry وعرض حقول الأثر والبناء والاختبارات ناجحة، والمقارنة البصرية والوصول الحي خارج الإثبات |
| 065 | `screens/065_admin_users.png` + `explanations/065_admin_users.md` | المستخدمون — إدارة — سطح مكتب واقعي — مرجع 23.19 | `apps/admin_web/client/src/components/AdminPeople.tsx`; `apps/admin_web/client/src/lib/admin-api.ts`; `apps/admin_web/server/admin-data.ts`; `docs/evidence/TASK_065_ADMIN_USERS_EVIDENCE.md`; `artifacts/task065_admin_users_result.json` | لا DB/API/RLS/صلاحيات جديدة؛ القائمة تستخدم `adminApi.users/deleteUser` مع pagination والـguards القائمة | دُققت قائمة الحسابات والبحث والترقيم والحذف وعرض الملف والمتجر. عُطلت أزرار البحث والتحديث أثناء loading وأضيف retry واضح عند فشل القراءة، مع بقاء تأكيد الحذف وحارس المستخدم الواحد وAdminSafeImage وحدود IP غير المسجل | loading، error/retry، empty، search، pagination، safe image، delete confirmation/guard، telemetry limits | `pnpm check`: PASS. `pnpm test`: PASS — 8 ملفات. `pnpm build`: PASS؛ تحذير chunk أكبر من 500 kB تحذير أداء فقط. لم تُنفذ عملية حذف Production حية | لا golden بصري: `065_admin_users.md/.png` غير موجودان في checkout أو شجرة GitHub المتاحة، لذلك لم يُنشأ baseline بديل ولم تُعلن مطابقة | GAP-039: فقدان Markdown/PNG يمنع المقارنة البصرية وإعادة قراءة العقد تفصيليًا. لا تغيير DB/API/RLS/الصلاحيات ولا ادعاء توفر IP أو مزامنة حية | PASS مع حدود موثقة — retry وحماية التوازي والبناء والاختبارات ناجحة، والمقارنة البصرية والحذف الحي خارج الإثبات |
| 066 | `screens/066_admin_notifications.png` + `explanations/066_admin_notifications.md` | الإشعارات الإدارية — إدارة — سطح مكتب واقعي — مرجع 23.20 | `apps/admin_web/client/src/components/AdminPeople.tsx`; `apps/admin_web/client/src/lib/admin-api.ts`; `apps/admin_web/server/admin-data.ts`; `docs/evidence/TASK_066_ADMIN_NOTIFICATIONS_EVIDENCE.md`; `artifacts/task066_admin_notifications_result.json` | لا DB/API/RLS/صلاحيات جديدة؛ الإشعارات تستخدم `adminApi.notifications/sendNotification/uploadPublicImage` والعقود القائمة | دُققت قائمة الإشعارات والإرسال لمستخدم أو للجميع ورفع الصورة. أضيفت حالة `error` مع retry ووسم تحديث وصولي، وتحقق MIME صريح لـPNG/JPEG/WebP قبل قراءة الملف، مع بقاء validation وحارس الإرسال وحارس الحجم | loading، error/retry، empty، user/broadcast target، MIME/size guard، send busy guard، image preview، success/error | `pnpm check`: PASS. `pnpm test`: PASS — 8 ملفات. `pnpm build`: PASS؛ تحذير chunk أكبر من 500 kB تحذير أداء فقط. لم تُنفذ عملية إرسال أو رفع Production حية | لا golden بصري: `066_admin_notifications.md/.png` غير موجودان في checkout أو شجرة GitHub المتاحة، لذلك لم يُنشأ baseline بديل ولم تُعلن مطابقة | GAP-040: فقدان Markdown/PNG يمنع المقارنة البصرية وإعادة قراءة العقد تفصيليًا. لا تغيير DB/API/RLS/الصلاحيات ولا ادعاء push أو مزامنة جهازية | PASS مع حدود موثقة — حالة المصدر والتحقق من MIME والبناء والاختبارات ناجحة، والإرسال/الرفع الحي والمقارنة البصرية خارج الإثبات |
| 067 | `screens/067_admin_analytics.png` + `explanations/067_admin_analytics.md` | الإحصاءات التشغيلية — إدارة — سطح مكتب واقعي — مرجع 23.21 | `apps/admin_web/client/src/components/AdminPeople.tsx`; `apps/admin_web/client/src/lib/admin-api.ts`; `apps/admin_web/server/admin-data.ts`; `docs/evidence/TASK_067_ADMIN_ANALYTICS_EVIDENCE.md`; `artifacts/task067_admin_analytics_result.json` | لا DB/API/RLS/صلاحيات جديدة؛ المؤشرات تستخدم `adminApi.analytics` وحسابات المصدر الحالية | دُققت مؤشرات المتاجر والمنتجات والطلبات والإشعارات وزمن القراءة. عُطلت إعادة القراءة أثناء الطلب، وأضيف retry للخطأ وحالة صريحة للرد `null` لمنع عرض إطار مؤشرات أو زمن غير صالح، مع بقاء البيانات مشتقة من Production دون تقدير | loading، error/retry، null response، operational counts، generatedAt، no estimated values | `pnpm check`: PASS. `pnpm test`: PASS — 8 ملفات. `pnpm build`: PASS؛ تحذير chunk أكبر من 500 kB تحذير أداء فقط. لم تُجرَ قراءة Analytics Production حية | لا golden بصري: `067_admin_analytics.md/.png` غير موجودان في checkout أو شجرة GitHub المتاحة، لذلك لم يُنشأ baseline بديل ولم تُعلن مطابقة | GAP-041: فقدان Markdown/PNG يمنع المقارنة البصرية وإعادة قراءة العقد تفصيليًا. لا تغيير DB/API/RLS/الصلاحيات ولا ادعاء قياس مشاهدات أو تحويلات غير موجودة | PASS مع حدود موثقة — حماية القراءة غير الصالحة والبناء والاختبارات ناجحة، والمقارنة البصرية والوصول الحي خارج الإثبات |
| 068 | `screens/068_state_loading.png` + `explanations/068_state_loading.md` | تحميل البيانات — كل الأدوار — لقطة حالة واقعية — مرجع 24 | `apps/mobile_flutter/lib/core/assal_widgets.dart` (جرد دون تعديل)؛ `apps/mobile_flutter/lib/features/customer/customer_discovery.dart`; `apps/mobile_flutter/lib/features/customer/customer_account.dart`; `apps/mobile_flutter/lib/features/customer/customer_catalog.dart`; `apps/mobile_flutter/lib/features/merchant/merchant_dashboard.dart`; `apps/mobile_flutter/test/task068_loading_state_test.dart`; `docs/evidence/TASK_068_LOADING_STATE_EVIDENCE.md`; `artifacts/task060_080_gap_baseline.json` (دليل غياب الأصل) | لا مصدر جديد ولا تغيير DB/API؛ المكونات المشتركة `AssalGlassLoading` و`AssalFutureStateView` و`AssalStateView` و`AssalMessageCard` تستخدم عقود `AssalLoadState` الحالية | جرى تدقيق مستهلكي العميل/التاجر والحساب والتفاصيل. Future المعلق يعرض loader موحدًا موسومًا بالعربية، والخطأ/الفراغ يمران عبر حالات مشتركة مع retry اختياري حقيقي. لم يثبت الجرد نقصًا إنتاجيًا يبرر إعادة بناء أو تغييرًا تجميليًا | loading موحد، Arabic label، Semantics live region، Future pending ثم Data، ومراجعة حدود action-level spinners المنفصلة | `flutter test test/task068_loading_state_test.dart`: PASS — اختباران. `flutter analyze --no-pub`: PASS — No issues found. لا اختبار golden لأن Markdown/PNG مفقودان | لا يوجد PNG/Markdown قابل لإعادة القراءة؛ لم يُصنع baseline بديل ولم تُعلن مطابقة بصرية. | `GAP-042`: فقدان أصول TASK 068 يمنع القبول البصري؛ القبول الوظيفي مثبت، ولا تغيير DB/API/RLS/permissions. الجاهزية الحية عبر أجهزة متعددة خارج هذا الاختبار | PASS وظيفيًا مع حجب بصري موثق — لا فجوة إنتاجية مثبتة في المكوّن المشترك، ومرجع 068 مفقود |
| 069 | `screens/069_state_empty_products.png` + `explanations/069_state_empty_products.md` | لا توجد منتجات — كل الأدوار — لقطة حالة واقعية — مرجع 24 | `apps/mobile_flutter/lib/core/assal_widgets.dart`; `apps/mobile_flutter/lib/features/customer/customer_discovery.dart`; `apps/mobile_flutter/test/task069_empty_products_test.dart`; `docs/evidence/TASK_069_EMPTY_PRODUCTS_EVIDENCE.md` | لا مصدر جديد ولا تغيير DB/API؛ الحالة تستخدم `AssalEmpty`/`AssalData` من `AssalRepository` و`DemoRepository` الاختباري فقط | أضيف دعم CTA اختياري حقيقي إلى `AssalStateView`/`AssalMessageCard`. الصفحة الرئيسية والرفوف تعرض إرشادًا عربيًا «استكشف المنتجات» مرتبطًا بالبحث، وشاشة البحث تعرض «مسح البحث والفلاتر» المرتبطًا بـ`_clearFilters` وإعادة التحميل. لا زر بلا callback ولا بيانات منتجات ثابتة | AssalEmpty، قائمة فارغة، رسالة موجهة، CTA حقيقي، مسح الفلاتر، retry، regression Home/Search/ProductCard | `flutter test test/task069_empty_products_test.dart`: PASS — اختباران. `flutter analyze --no-pub`: PASS. regressions الوظيفية TASK 007/TASK 010/ProductCard: PASS. goldens السابقة محجوبة ولم تُحدّث | لا PNG/Markdown قابل لإعادة القراءة؛ لم يُصنع baseline بديل ولم تُعلن مطابقة بصرية | `GAP-043`: فقدان أصول TASK 069 يمنع القبول البصري. لا تغيير DB/API/RLS/permissions؛ المزامنة الحية خارج الإثبات | PASS وظيفيًا مع حجب بصري موثق — حالات الفراغ والإجراء البديل حقيقية ومختبرة |
| 070 | `screens/070_state_empty_stores.png` + `explanations/070_state_empty_stores.md` | لا توجد متاجر — كل الأدوار — لقطة حالة واقعية — مرجع 24 | `apps/mobile_flutter/lib/features/customer/customer_discovery.dart`; `apps/mobile_flutter/test/task070_empty_stores_test.dart`; `apps/mobile_flutter/test/task014_stores_list_test.dart`; `docs/evidence/TASK_070_EMPTY_STORES_EVIDENCE.md` | لا مصدر جديد ولا تغيير DB/API؛ `StoresScreen` يستخدم `listStores` وعقود `AssalStoreSummary` الحالية | فُصل فراغ المصدر عن عدم تطابق البحث/الفلاتر. فراغ المصدر يعرض رسالة منشورة مع «مسح البحث والفلاتر» المرتبط بـ`_resetFilters` وإعادة القراءة؛ عدم التطابق يعرض رسالة مخصصة مع الإجراء نفسه. بقي فتح المتجر والتحديث والفلاتر الحقيقية دون بيانات ثابتة | empty source، filtered empty، retry، refresh، search، filter reset، source-backed store profile، regression | `flutter test test/task070_empty_stores_test.dart`: PASS — اختباران. `flutter analyze --no-pub`: PASS. regressions الوظيفية TASK 069 وTASK 014 وTASK 009: PASS. فشلت goldens المعروفة فقط ولم تُحدّث | لا PNG/Markdown قابل لإعادة القراءة؛ لم يُصنع baseline بديل ولم تُعلن مطابقة بصرية | `GAP-044`: فقدان أصول TASK 070 يمنع القبول البصري. لا تغيير DB/API/RLS/permissions؛ المزامنة الحية خارج الإثبات | PASS وظيفيًا مع حجب بصري موثق — رسائل فراغ المتاجر والاسترداد مرتبطة بالمصدر الحقيقي |
| 071 | `screens/071_state_empty_requests.png` + `explanations/071_state_empty_requests.md` | لا توجد طلبات — كل الأدوار — لقطة حالة واقعية — مرجع 24 | `apps/mobile_flutter/lib/features/customer/customer_account.dart`; `apps/mobile_flutter/test/task071_empty_requests_test.dart`; `docs/evidence/TASK_071_EMPTY_REQUESTS_EVIDENCE.md` | لا مصدر جديد ولا تغيير DB/API؛ `RequestsScreen` يستخدم `getSession` و`listRequests(userId)` وعقود الطلبات الحالية | عولج مسار `AssalEmpty` المصدرية ليعرض نفس حالة القائمة الفارغة المخصصة، مع مرشح حالة، retry يعيد `listRequests`، و«استكشف المنتجات» إلى `SearchScreen` الحقيقي. بقي الضيف خلف تسجيل الدخول وحالة الجلسة غير المتاحة خلف رسالة مزامنة وretry | authenticated empty، AssalEmpty، status filters، retry، explore products، guest boundary، session unavailable | `flutter test test/task071_empty_requests_test.dart`: PASS — اختبار واحد. `flutter analyze --no-pub`: PASS. regressions الوظيفية TASK 024 وTASK 025: PASS. goldens المعروفة محجوبة ولم تُحدّث | لا PNG/Markdown قابل لإعادة القراءة؛ لم يُصنع baseline بديل ولم تُعلن مطابقة بصرية | `GAP-045`: فقدان أصول TASK 071 يمنع القبول البصري. لا تغيير DB/API/RLS/permissions؛ المزامنة الحية خارج الإثبات | PASS وظيفيًا مع حجب بصري موثق — فراغ الطلبات المصادق عليه لا يختلط بحالة الضيف |
| 072 | `screens/072_state_empty_messages.png` + `explanations/072_state_empty_messages.md` | لا توجد رسائل — كل الأدوار — لقطة حالة واقعية — مرجع 24 | `apps/mobile_flutter/lib/features/customer/customer_account.dart` (تدقيق دون تعديل); `apps/mobile_flutter/test/task072_empty_messages_test.dart`; `docs/evidence/TASK_072_EMPTY_MESSAGES_EVIDENCE.md` | لا مصدر جديد ولا تغيير DB/API؛ القائمة تستخدم `listConversations` وسجل المحادثة يستخدم `listMessages` والإرسال يستخدم `sendMessage` من العقود القائمة | جرى تثبيت أن قائمة المحادثات تعرض فراغًا مخصصًا مع «استكشف المتاجر» إلى `StoresScreen` الحقيقي، وأن سجل المحادثة يعرض رسالة فراغ عربية مع محرر إرسال للمستخدم المصادق عليه. أضيف اختبار مستقل للفراغين دون إعادة بناء غير مبررة | conversation empty، message history empty، explore stores، composer، authenticated boundary، retry/error path، source-backed actions | `flutter test test/task072_empty_messages_test.dart`: PASS — اختباران. `flutter analyze --no-pub`: PASS. regressions الوظيفية TASK 026 لقائمة المحادثات والحقول المصدرية: PASS. لا تعديل إنتاجي لعدم ثبوت فجوة سلوكية | لا PNG/Markdown قابل لإعادة القراءة؛ لم يُصنع baseline بديل ولم تُعلن مطابقة بصرية | `GAP-046`: فقدان أصول TASK 072 يمنع القبول البصري. لا تغيير DB/API/RLS/permissions؛ المزامنة الحية خارج الإثبات | PASS وظيفيًا مع حجب بصري موثق — التدقيق والاختبار يثبتان مسارات الفراغ القائمة |
| 073 | `screens/073_state_empty_notifications.png` + `explanations/073_state_empty_notifications.md` | لا توجد إشعارات — كل الأدوار — لقطة حالة واقعية — مرجع 24 | `apps/mobile_flutter/lib/features/customer/customer_account.dart` (تدقيق دون تعديل); `apps/mobile_flutter/test/task073_empty_notifications_test.dart`; `docs/evidence/TASK_073_EMPTY_NOTIFICATIONS_EVIDENCE.md` | لا مصدر جديد ولا تغيير DB/API؛ الإشعارات تستخدم `getSession` و`listNotifications` و`markNotificationRead` الحالية | جرى تثبيت مساري `AssalEmpty` والقائمة الفارغة برسالة «لا توجد إشعارات جديدة الآن.» وإجراء «استكشف السوق» المرتبط بـ`HomeScreen` الحقيقي. بقيت بوابة الضيف وretry للجلسة وعمليات القراءة المصدرية دون إعادة بناء غير مبررة | empty notifications، explore market، guest auth gate، session retry، mark read، mark all read، source-backed action | `flutter test test/task073_empty_notifications_test.dart`: PASS — اختبار واحد. `flutter analyze --no-pub`: PASS. regressions الوظيفية TASK 033 للقراءة وحدود الضيف وretry: PASS. لم تُشغّل goldens المعروفة كقبول بصري | لا PNG/Markdown قابل لإعادة القراءة؛ لم يُصنع baseline بديل ولم تُعلن مطابقة بصرية | `GAP-047`: فقدان أصول TASK 073 يمنع القبول البصري. لا تغيير DB/API/RLS/permissions؛ push والمزامنة الحية خارج الإثبات | PASS وظيفيًا مع حجب بصري موثق — الجرد والاختبار يثبتان الحالة المصدرية والإجراء الحقيقي |
| 074 | `screens/074_state_empty_saved.png` + `explanations/074_state_empty_saved.md` | لا توجد محفوظات — عميل — لقطة حالة واقعية — مرجع 24 | `apps/mobile_flutter/lib/features/customer/customer_favorites.dart`; `apps/mobile_flutter/test/task074_empty_saved_test.dart`; `apps/mobile_flutter/test/task029_saved_items_test.dart`; `docs/evidence/TASK_074_EMPTY_SAVED_EVIDENCE.md` | لا مصدر جديد ولا تغيير DB/API؛ المحفوظات تستخدم `listFavoriteProducts` و`listFollowedStores` و`listFavoriteTaxonomies` الحالية | أضيفت رسالة مخصصة وCTA «استكشف المنتجات» حقيقي لتبويب التصنيفات المرتبطة عندما تكون `AssalEmpty` أو القائمة فارغة، مع بقاء تبويبي المنتجات والمتاجر ومسارات الإزالة والـretry المصدرية. استُكمل stub regression للمحفوظات بعقود البحث اللازمة، وصُحح توقع نص SearchScreen إلى النص الفعلي | saved products empty، followed stores empty، related taxonomy empty، CTA، auth gate، retry، remove actions، SearchScreen route contract | `flutter test test/task074_empty_saved_test.dart`: PASS — اختبار واحد. `flutter analyze --no-pub`: PASS. regressions TASK 029 وTASK 030 لحالات الاستكشاف الفارغة: PASS. لم تُحدّث goldens | لا PNG/Markdown قابل لإعادة القراءة؛ لم يُصنع baseline بديل ولم تُعلن مطابقة بصرية | `GAP-048`: فقدان أصول TASK 074 يمنع القبول البصري. لا تغيير DB/API/RLS/permissions؛ مزامنة المحفوظات الحية خارج الإثبات | PASS وظيفيًا مع حجب بصري موثق — حالات المحفوظات الثلاث مرتبطة بعقود المصدر وإجراءات حقيقية |
| 075 | `screens/075_state_empty_following.png` + `explanations/075_state_empty_following.md` | لا توجد متابعات — عميل — لقطة حالة واقعية — مرجع 24 | لم يبدأ | لم يبدأ | لم يبدأ | لم يبدأ | لم يبدأ | لم يبدأ | لم يبدأ | لم يبدأ |
| 076 | `screens/076_state_connection_error.png` + `explanations/076_state_connection_error.md` | فشل الاتصال وإعادة المحاولة — كل الأدوار — لقطة حالة واقعية — مرجع 24 | لم يبدأ | لم يبدأ | لم يبدأ | لم يبدأ | لم يبدأ | لم يبدأ | لم يبدأ | لم يبدأ |
| 077 | `screens/077_state_session_expired.png` + `explanations/077_state_session_expired.md` | انتهاء الجلسة — كل الأدوار — لقطة حالة واقعية — مرجع 24 | لم يبدأ | لم يبدأ | لم يبدأ | لم يبدأ | لم يبدأ | لم يبدأ | لم يبدأ | لم يبدأ |
| 078 | `screens/078_state_upload_error.png` + `explanations/078_state_upload_error.md` | فشل رفع الصورة — تاجر / عميل — لقطة حالة واقعية — مرجع 24 | لم يبدأ | لم يبدأ | لم يبدأ | لم يبدأ | لم يبدأ | لم يبدأ | لم يبدأ | لم يبدأ |
| 079 | `screens/079_state_comment_pending.png` + `explanations/079_state_comment_pending.md` | تعليق قيد المراجعة — عميل — لقطة حالة واقعية — مرجع 24 | لم يبدأ | لم يبدأ | لم يبدأ | لم يبدأ | لم يبدأ | لم يبدأ | لم يبدأ | لم يبدأ |
| 080 | `screens/080_state_review_pending.png` + `explanations/080_state_review_pending.md` | تقييم قيد المراجعة — عميل — لقطة حالة واقعية — مرجع 24 | لم يبدأ | لم يبدأ | لم يبدأ | لم يبدأ | لم يبدأ | لم يبدأ | لم يبدأ | لم يبدأ |
| 081 | `screens/081_state_request_success.png` + `explanations/081_state_request_success.md` | نجاح الرد على الطلب — عميل / تاجر — لقطة حالة واقعية — مرجع 24 | لم يبدأ | لم يبدأ | لم يبدأ | لم يبدأ | لم يبدأ | لم يبدأ | لم يبدأ | لم يبدأ |

## ملاحظات عامة على الإطلاق

لن يُبنى APK/AAB نهائي أو تُعلن جاهزية إنتاجية قبل إغلاق المهام ذات الصلة، اجتياز بوابات Flutter/Admin، وفحص الفجوات. لا توجد في هذه المرحلة مطالبة بتغيير قاعدة البيانات أو API أو RLS أو الصلاحيات.
