# دليل القبول البصري — لوحة الإدارة — 2026-08-19

تم فتح `http://127.0.0.1:3210` على متصفح المستخدم المحلي والتحقق من أن لوحة الإدارة تُحمّل بعد حالة التحقق من الهوية، وتعرض الهوية الإدارية `admin.local@gvalqfgxrkibuydoiuiz.internal` بدور «مدير عام» ومصدر البيانات «Supabase Production».

ظهرت قائمة التنقل العربية كاملة، بما فيها «توثيق المتاجر Pro»، «التصنيفات»، و«التوصيل والاستلام». كما ظهرت حالة التشغيل التي تؤكد عدم استخدام بيانات Demo في المسار الإداري، وظهرت أرقام Production فعلية: مستخدمان، منتج واحد، متجر واحد، و357 منطقة مرجعية.

الملاحظة البصرية أثناء الالتقاط: القائمة الجانبية قابلة للتمرير داخل حاويتها، لكن لقطة الشاشة الأولى أظهرت الجزء العلوي فقط؛ يلزم اختبار النقر على «التوصيل والاستلام» والتمرير إلى العناصر السفلى في جلسة قبول لاحقة للتأكد من عدم وجود قص بصري على جميع أحجام النوافذ.

تم اختبار تمرير الصفحة/القائمة ومحاولة تحديد «التوصيل والاستلام». النص موجود في DOM والقائمة تحتوي عليه، لكن اللقطة بعد التمرير بقيت عند العناصر العلوية؛ لذلك يحتاج النقر البصري على العنصر السفلي إلى تمرير موجه داخل حاوية القائمة نفسها أو اختبار يدوي من المستخدم على جهازه. لا يوجد دليل على شاشة بيضاء أو فقدان العنصر من DOM.

تم التحقق أيضًا من أن الضغط على «نظرة عامة» يعمل ويعيد تحميل محتوى Production دون شاشة بيضاء، وأن القائمة الجانبية تبقى ظاهرة مع شريط تمرير. لم يمكن الوصول بصريًا إلى عنصر «التوصيل والاستلام» عبر فهرس العناصر الظاهرة لأنه خارج viewport، رغم ظهوره في DOM؛ لذلك تبقى هذه نقطة قبول يدوية/موجهة وليست فشلًا وظيفيًا مثبتًا.

تم بدء اختبار التنقل بالكيبورد من «نظرة عامة» للتحقق من إمكانية الوصول إلى العناصر خارج viewport. لم تظهر شاشة خطأ أو فقدان جلسة أثناء الاختبار.

كشفت محاولة فتح `/api/admin/delivery-methods` مباشرة قبل إعادة التشغيل أن العملية المحلية كانت تستخدم `dist` قديمًا؛ ظهر 404 من واجهة SPA رغم أن المصدر الجديد يحتوي المسار. أُعيد تشغيل الخادم المحلي على Windows بعد بناء المرحلة 7، وظهر في السجل `Admin server running on http://127.0.0.1:3210/`. يجب إعادة اختبار endpoint بعد هذا التشغيل، ولا تُفسر نتيجة 404 القديمة كفشل في الكود الجديد.

بعد إعادة التشغيل، نجح endpoint `/api/admin/delivery-methods` وأعاد ثلاث طرق Production: «استلام من المتجر»، «توصيل التاجر»، و«شركة توصيل». كما أعاد `/api/admin/stores?page=1&pageSize=10` متجرًا حقيقيًا بمعرف `32fb6bdd-ceb4-4a8b-b6a1-1ac92c466c47` وحالة `active`. واختبار `/api/admin/stores/32fb6bdd-ceb4-4a8b-b6a1-1ac92c466c47/logistics` نجح وأعاد `deliveryMethods: []`, `deliveryOptions: []`, `pickupLocations: []`, `regions: []`؛ وهذا Empty State حقيقي صحيح وليس خطأ اتصالًا.
