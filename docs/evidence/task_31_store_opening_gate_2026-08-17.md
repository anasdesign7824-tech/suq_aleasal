# Task 31 — فتح المتجر

## النطاق

تحسين نموذج فتح المتجر الحالي دون بناء Store Wizard كامل. التغيير يركز على التحقق الواضح، واستعادة المسودة داخل Repository، ورسائل عربية قابلة للفهم، مع بقاء الإرسال منفصلًا عن الحفظ.

## التنفيذ

تم تحويل الحقول الأساسية إلى `TextFormField` مع تحقق مستقل لاسم النشاط، ورقم التواصل، والخبرة، والموقع، والتخصصات. عند وجود خطأ تظهر رسالة مرتبطة بالحقل، وعند محاولة الإرسال تظهر رسالة عامة قصيرة تطلب مراجعة الحقول.

أُضيفت عمليات Repository التالية:

| العملية | Demo | Production |
|---|---|---|
| تحميل المسودة | تستعيد المسودة من الذاكرة أثناء جلسة التطبيق | `BLOCKED` برسالة `production_merchant_draft_not_configured` |
| حفظ المسودة | يحفظ `AssalMerchantApplicationDraft` للمستخدم | `BLOCKED` صريح، بلا نجاح وهمي |
| مسح المسودة | يمسحها بعد نجاح الإرسال | `BLOCKED` صريح |
| إرسال الطلب | ينشئ ملخص طلب وإشعار Demo بعد تحقق العقد | `BLOCKED` حتى تهيئة مصدر الإنتاج والمراجعة الإدارية |

لا يدّعي النص حفظًا دائمًا على الجهاز؛ بل يوضح أن المسودة تعمل أثناء جلسة التطبيق الحالية. زر «حفظ المسودة الآن» فعّال، وعند الإرسال الناجح تُمسح المسودة وتظهر حالة الطلب الفعلية من العقد بدل نص حالة ثابت.

## الحدود المعمارية

تبقى الشاشة ضمن UI، وتستخدم `AssalRepository` لكل عمليات المسودة والإرسال. لم تتم إضافة Supabase أو Gateway داخل Widget، ولم يُنشأ نجاح إنتاجي وهمي.

## الاختبارات

| الفحص | النتيجة |
|---|---|
| `dart format` للملفات المعدلة | PASS |
| `flutter analyze` | PASS — No issues found |
| اختبارات `navigation_test.dart` و`data_layer_test.dart` و`customer_journey_test.dart` | PASS — 6 tests passed |
| اختبار مستقل لحفظ واستعادة ومسح Draft | PASS |
| `git diff --check` | PASS |
| تشغيل Demo على `emulator-5554` | PASS — build/install وظهور `com.assalkom.assalkom/.MainActivity` |

## Visual QA

تم بناء وتثبيت نسخة Debug على المحاكي المحلي بنجاح. تعذر استخراج لقطة شاشة صالحة عبر ADB؛ أداة `screencap` أعادت ملفًا صفري الحجم رغم أن Activity كانت مرئية. لذلك لا أعتبر لقطة غير صالحة دليلًا بصريًا، وتبقى مراجعة اللقطات التفاعلية الكاملة **BLOCKED** إلى حين توفر مسار Screenshot يعمل أو مراجعة جهاز Mimo المتصل.

## بوابة القبول

**BLOCKED** — التنفيذ البرمجي، حدود البيانات، التحليل، الاختبارات، والبناء/التثبيت ناجحة. سبب الحجب الوحيد هو عدم اكتمال الدليل البصري التفاعلي بسبب فشل ADB Screenshot، مع إبقاء Production Draft صريحًا في حالة غير مهيأة وعدم ادعاء جاهزية إنتاجية.
